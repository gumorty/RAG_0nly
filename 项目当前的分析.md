我看了你最新的 `flow_6_27_1/RAG` 分支，也结合你截图里的界面状态来看，当前系统的主链路已经比前一个分支清晰很多：README 里已经明确当前是“管理平台 + RAGFlow 引擎”，管理平台负责账号、权限、知识库、模型路由、审计和前端交互，RAGFlow 负责 DeepDOC 解析、切块、Embedding、索引和检索。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_1/RAG))
但是你提到的 4 个问题确实都存在，而且它们不是简单 UI 问题，而是会直接影响这个项目能不能真正变成一个可落地的企业级 RAG 系统。

------

# 一、当前最大问题：知识库下面没有真正的“会话管理”

## 1. 当前代码现状

现在前端只有：

```ts
listAnswers(collectionId, limit = 20)
```

也就是说，前端切换知识库后，只是按 `collection_id` 拉取最近 20 条 `answers`，然后还原成聊天记录。`api.ts` 里目前有 `knowledgeGaps`、`listCollections`、`listDocuments`、`listAnswers`、`meetingSummary`、`chat`、`chatStream` 等接口，但没有 `listSessions`、`createSession`、`deleteSession`、`exportSession` 这类会话接口。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/frontend/app/api.ts))

后端也是一样，`GET /collections/{collection_id}/answers` 当前只按 `collection_id` 查询 `Answer` 表，取最近 limit 条返回，没有 `session_id`、`user_id`、`conversation_id` 这些维度。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/api/routes.py))

数据库层面，目前有 `Answer` 表，保存的是：

```text
collection_id
question
answer
citations
model
evidence_score
feedback
created_at
```

但是没有 `chat_sessions` 和 `chat_messages` 表。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/models/entities.py))

你虽然有一个 `RagflowSession` 表，但它更像是 RAGFlow 自己的 chat/session 映射，字段包括 `collection_id`、`chat_id`、`session_id`、`user_id`、`turn_count`、`title` 等，不是你前端可管理的业务会话模型。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/models/entities.py))

所以现在准确来说是：

```text
一个知识库 = 一个隐式聊天流
聊天记录 = 这个知识库最近 20 条 answers
```

这不是企业级知识库应该有的会话系统。

------

## 2. 应该怎么改

你需要新增两个核心表：

```python
class ChatSession(Base):
    __tablename__ = "chat_sessions"

    id = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    collection_id = mapped_column(String(36), index=True)
    user_id = mapped_column(String(36), index=True)
    title = mapped_column(String(300), default="新会话")
    summary = mapped_column(Text, nullable=True)
    status = mapped_column(String(40), default="active")  # active / archived / deleted
    message_count = mapped_column(Integer, default=0)
    last_message_at = mapped_column(DateTime, nullable=True)
    metadata_ = mapped_column("metadata", JSON, default=dict)
    created_at = mapped_column(DateTime, default=datetime.utcnow)
    updated_at = mapped_column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)


class ChatMessage(Base):
    __tablename__ = "chat_messages"

    id = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    session_id = mapped_column(String(36), index=True)
    collection_id = mapped_column(String(36), index=True)
    user_id = mapped_column(String(36), index=True)
    role = mapped_column(String(30))  # user / assistant / system
    content = mapped_column(Text)
    answer_id = mapped_column(String(36), nullable=True, index=True)
    trace_id = mapped_column(String(36), nullable=True, index=True)
    citations = mapped_column(JSON, default=list)
    evidence_score = mapped_column(Float, default=0.0)
    token_count = mapped_column(Integer, default=0)
    created_at = mapped_column(DateTime, default=datetime.utcnow)
```

然后新增 API：

```text
GET    /collections/{collection_id}/sessions
POST   /collections/{collection_id}/sessions
GET    /sessions/{session_id}/messages
PATCH  /sessions/{session_id}
DELETE /sessions/{session_id}
GET    /sessions/{session_id}/export?format=md|json
POST   /chat
POST   /chat/stream
```

`/chat` 和 `/chat/stream` 的 payload 必须从现在的：

```json
{
  "collection_id": "...",
  "question": "...",
  "history": []
}
```

改成：

```json
{
  "collection_id": "...",
  "session_id": "...",
  "question": "...",
  "history": []
}
```

这样才能做到：

```text
知识库 A
  ├── 会话 1：项目资料问答
  ├── 会话 2：制度文档问答
  ├── 会话 3：论文资料分析
```

------

## 3. 前端应该怎么改

你截图里左侧现在是“知识库列表”，中间是一个固定对话窗口。建议改成：

```text
左侧：
- 知识库列表
  - 企业知识库
    - 新建会话按钮
    - 最近会话 1
    - 最近会话 2
    - 最近会话 3
```

每个会话右侧菜单提供：

```text
重命名
导出 Markdown
导出 JSON
归档
删除
```

前端状态从：

```ts
const [messages, setMessages] = useState<Message[]>([]);
const [selectedId, setSelectedId] = useState<string>();
```

升级成：

```ts
const [selectedCollectionId, setSelectedCollectionId] = useState<string>();
const [selectedSessionId, setSelectedSessionId] = useState<string>();
const [sessions, setSessions] = useState<ChatSession[]>([]);
const [messages, setMessages] = useState<ChatMessage[]>([]);
```

切换知识库时：

```text
1. 加载该知识库的 sessions
2. 默认选中最近一个 session
3. 如果没有 session，自动创建一个
4. 再加载该 session 的 messages
```

不要再按 `collection_id` 直接拉最近 20 条 `answers` 当聊天记录。

------

# 二、“证据不足 / 缺口运营”现在只是展示，没有闭环

## 1. 当前代码现状

现在 `knowledgeGaps` 前端接口只是调用：

```ts
GET /knowledge-gaps?collection_id=...
```

并且有一个 `answerToEvalCase(answerId)`，能把某条回答转成评测用例。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/frontend/app/api.ts))

后端 `GET /knowledge-gaps` 当前是从 `Answer` 表倒序查询回答，然后对每条 answer 调 `_gap_reason(answer)`，如果有 reason，就返回 gap。也就是说，当前缺口不是一张独立表，而是从历史回答中临时推导出来的列表。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/api/routes.py))

低证据的统计目前也很简单，后台 metrics 里直接用：

```python
answer.evidence_score < 0.22
```

来统计低证据回答数量。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/api/routes.py))

ChatService 里 RAGFlow retrieval-only 路径的证据分也是：

```python
evidence_score = max([chunk.score for chunk in chunks], default=0.0)
```

也就是说，目前证据不足主要由 RAGFlow 返回 chunk 的最大相似度决定。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/services/chat.py))

这就解释了你截图里“证据不足 / 缺口运营”的问题：它现在更像一个告警列表，不是真正的运营闭环。

------

## 2. 当前判断证据不足的方式有什么问题

现在的判断太单一。

它基本依赖：

```text
最大 chunk 相似度
引用数量
feedback
```

但真实情况复杂得多。比如用户问题在知识库里是相关的，但系统仍然判断证据不足，可能有这些原因：

```text
1. query rewrite 改写错了。
2. RAGFlow 召回到了表格 HTML 片段，但相似度低。
3. 文档被切块切坏了，相关信息分散在多个 chunk。
4. 问题需要跨文档、多跳推理，但当前只召回局部证据。
5. ACL 后置过滤后，把本来相关的 chunk 过滤掉了。
6. 用户问的是表格数值类问题，向量检索不擅长精确匹配。
7. 用户用了同义词、简称、错别字，关键词没命中。
```

当前 README 也承认两个相关限制：表格型 PDF 的部分引用片段仍可能显示为 HTML table，ACL 目前是后置过滤，理想方案是在 RAGFlow 检索层支持 metadata filter 或按权限拆 dataset。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_1/RAG))

所以，“证据不足”不能只展示，必须能被处理。

------

## 3. 应该新增真正的 KnowledgeGap 表

建议新增：

```python
class KnowledgeGap(Base):
    __tablename__ = "knowledge_gaps"

    id = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    collection_id = mapped_column(String(36), index=True)
    session_id = mapped_column(String(36), nullable=True, index=True)
    answer_id = mapped_column(String(36), nullable=True, index=True)
    trace_id = mapped_column(String(36), nullable=True, index=True)

    question = mapped_column(Text)
    rewritten_query = mapped_column(Text, nullable=True)

    gap_type = mapped_column(String(50), index=True)
    # low_evidence / no_citation / user_marked / retrieval_miss /
    # acl_filtered / table_parse_issue / query_rewrite_issue

    status = mapped_column(String(40), default="open", index=True)
    # open / triaged / resolved / ignored / eval_case

    severity = mapped_column(String(20), default="medium")
    reason = mapped_column(Text)
    evidence_score = mapped_column(Float, default=0.0)

    expected_document_ids = mapped_column(JSON, default=list)
    expected_chunk_ids = mapped_column(JSON, default=list)
    resolution_note = mapped_column(Text, nullable=True)

    created_by = mapped_column(String(36), nullable=True, index=True)
    resolved_by = mapped_column(String(36), nullable=True)
    created_at = mapped_column(DateTime, default=datetime.utcnow)
    resolved_at = mapped_column(DateTime, nullable=True)
```

------

## 4. 缺口运营界面应该提供哪些动作

你现在右侧不能只列“证据不足”，应该加这些按钮：

```text
1. 标记为误判
2. 重新检索
3. 加入评测集
4. 关联正确文档
5. 补充资料
6. 忽略
7. 标记已解决
```

具体流程：

### 场景 A：系统误判证据不足

用户点击：

```text
标记为误判
```

系统弹出：

```text
你认为哪些文档可以回答这个问题？
[选择文档]
[填写正确答案要点]
[加入评测集]
```

后台做：

```text
1. feedback = incorrect_low_evidence
2. 创建 KnowledgeGap
3. 创建 EvalCase
4. 记录 expected_document_ids / expected_chunk_ids
5. 后续跑评测时必须命中这些文档
```

你已经有 `/answers/{answer_id}/feedback` 和 `/answers/{answer_id}/to-eval-case` 基础接口，可以在这个基础上扩展。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/api/routes.py))

### 场景 B：确实缺资料

用户点击：

```text
补充资料
```

系统跳转到上传区域，并自动带上：

```text
问题
缺失主题
建议补充资料类型
```

例如：

```text
这个问题缺少“2025 差旅住宿标准调整说明”相关资料。
建议上传：制度文件、Excel 标准表、审批说明。
```

### 场景 C：召回错了

用户点击：

```text
重新检索
```

可以提供策略：

```text
扩大 top_k
降低 similarity_threshold
启用关键词优先
启用 reranker
只检索指定文档
检索表格内容
```

------

## 5. 证据分应该升级为多维评分

不要只用 `max(chunk.score)`。

建议改成：

```text
final_evidence_score =
0.30 * max_similarity
+ 0.20 * rerank_score
+ 0.15 * keyword_or_entity_coverage
+ 0.15 * citation_count_quality
+ 0.10 * unique_document_coverage
+ 0.10 * answer_grounding_score
```

并且 trace 里保存：

```json
{
  "score_breakdown": {
    "max_similarity": 0.57,
    "rerank_score": null,
    "keyword_coverage": 0.45,
    "citation_count": 3,
    "unique_doc_count": 2,
    "acl_filtered_from": 8,
    "acl_filtered_to": 3
  }
}
```

你的 ChatService 已经在 trace 的 strategy 里记录了 `acl_filtered_from` 和 `acl_filtered_to`，这是好事，后续可以直接展示在“召回诊断”里。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/services/chat.py))

------

# 三、右侧“资料摘要”目前作用太弱

## 1. 当前代码现状

前端有：

```ts
meetingSummary(collectionId, meetingDate?)
```

调用的是：

```text
GET /collections/{collection_id}/meeting-summary
```

([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/frontend/app/api.ts))

后端 `meeting_summary` 的逻辑是：查当前知识库下的文档，按 ACL 过滤，然后从每个文档的 metadata 里取：

```python
document.metadata_.analysis.lab_signals
```

再聚合：

```text
authors
projects
progress
risks
next_steps
decisions
key_points
```

([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/api/routes.py))

所以当前“资料摘要”本质上不是一个真正的动态资料摘要，也不是基于当前问题的 RAG 摘要，而是：

```text
从文档 analysis.lab_signals 中聚合出来的周报 / 会议型摘要
```

这解释了你为什么感觉它只是展示：它没有参与检索，没有参与问答，也不能点击进入证据链。

------

## 2. 它应该起什么作用

“资料摘要”应该升级成“知识库洞察 / Knowledge Insights”，它应该服务 3 个目标：

```text
1. 帮用户快速理解这个知识库有什么内容。
2. 帮用户快速发起高质量问题。
3. 作为检索召回的上层索引，提升复杂问题召回率。
```

也就是说，它不应该只是右侧展示，而应该变成：

```text
知识库概览
+ 可点击证据
+ 问题推荐
+ 摘要索引
+ Agent context pack
```

------

## 3. 资料摘要应该按文档类型分模板

现在的 `meeting-summary` 偏周报/会议，不适合所有文档。

应该改成：

```text
collection_insights
document_summaries
```

不同文档类型不同摘要模板：

### 周报 / 会议纪要

```text
本周进展
风险阻塞
下周计划
关键决策
负责人
```

### 论文

```text
研究问题
方法
数据集
指标
实验结果
局限
可复现细节
```

### 制度文件

```text
适用范围
关键条款
审批流程
金额标准
责任部门
例外情况
```

### Excel / 表格

```text
Sheet 列表
关键字段
数值范围
地区/部门/时间维度
异常值
```

### 代码文件

```text
模块职责
入口函数
类和方法
依赖关系
配置项
测试方式
```

------

## 4. 资料摘要应该怎么和问答结合

右侧摘要里的每一条都应该支持：

```text
点击追问
展开证据
加入上下文
导出报告
生成评测问题
```

例如摘要中有：

```text
风险：RAGFlow 表格型 PDF 引用仍显示 HTML table 片段
```

点击后自动在输入框生成：

```text
请根据资料说明：RAGFlow 表格型 PDF 引用显示 HTML table 的问题是什么？应该如何优化？请给出引用。
```

或者直接触发检索：

```text
query = "表格型 PDF HTML table 引用 结构化 渲染 优化"
```

这样“资料摘要”才从展示组件变成知识入口。

------

# 四、核心质量：问答必须高召回、高准确、支持多格式文档

这是你这个项目最核心的一点。

你现在的 README 已经写明主链路是：上传资料后，必要时把 Excel / CSV / HTML / PPTX 等格式标准化为 RAGFlow 友好内容，然后 RAGFlow 执行 DeepDOC 解析、分块、Embedding 和索引；问答时优先调用 RAGFlow retrieval 获取证据，后端按 ACL 过滤 chunks，再调用 Chat 模型生成答案。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_1/RAG))

这个方向是对的，但“能上传”和“能高质量问答”不是一回事。

------

## 1. 当前多格式处理的缺口

旧本地 parser 支持：

```text
.pdf
.docx
.doc
.pptx
.xlsx / .xlsm
.html / .htm
.md / .markdown
.txt
.csv
.json
```

代码里确实能看到这些分支。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/rag/parsers.py))

但是它没有专门支持：

```text
.xls 老式 Excel
.ppt 老式 PPT
图片
扫描 PDF OCR
主流代码文件的语义切分
表格结构化
JSONPath-aware 解析
```

现有 xlsx 解析只是逐行拼成 `" | "` 分隔文本。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/rag/parsers.py))
这对普通展示可以，但对表格问答不够，因为模型很容易不知道某个数值对应哪一列、哪个 sheet、哪一行。

------

## 2. 应该设计“文档类型路由器”

你需要一个 `DocumentProcessingRouter`：

```text
PDF 普通文档
→ RAGFlow DeepDOC

扫描 PDF / 图片
→ OCR + 版面分析 + 图片描述

Excel / CSV
→ 表格结构化解析 → row-level markdown/jsonl

JSON / YAML
→ path-aware flatten，保留 JSONPath

代码文件
→ language-aware parser，按 class/function/module 切块

PPT / PPTX
→ slide-level 解析 + notes + 图片 OCR

Markdown / TXT
→ heading-aware chunk
```

------

## 3. Excel 应该怎么处理

不要直接把 Excel 行拼成：

```text
北京 | 1100 | 650 | 500
```

应该转成：

```text
Sheet: 中央和国家机关国内差旅住宿费标准调整表
Row: 12
地区: 北京市
部级住宿费标准: 1100 元/人天
司局级住宿费标准: 650 元/人天
其他人员住宿费标准: 500 元/人天
旺季上浮: 无
```

这样用户问：

```text
北京市司局级住宿费是多少？
```

系统能精确召回对应字段，而不是召回一整块混乱表格。

------

## 4. 代码文件应该怎么处理

如果要支持主流语言代码，不能只按文本切块。

代码应该提取：

```text
language
file_path
module
class
function
signature
docstring
imports
called_functions
config_keys
```

代码 chunk 示例：

```json
{
  "type": "code_symbol",
  "language": "python",
  "file_path": "backend/app/services/chat.py",
  "symbol_type": "function",
  "symbol_name": "_filter_ragflow_chunks_by_acl",
  "signature": "...",
  "content": "...",
  "dependencies": ["Document", "select"]
}
```

这样 Agent 或用户问：

```text
RAGFlow 检索结果是怎么做 ACL 过滤的？
```

系统能直接命中 `_filter_ragflow_chunks_by_acl`，而不是召回整份代码文件。

------

## 5. 图片和扫描件应该怎么处理

图片类资料需要：

```text
OCR 文本
图片 caption
版面坐标
表格区域
图表说明
截图中的 UI 元素
```

对于论文、PPT、制度扫描件，这个非常重要。

------

## 6. 检索链路也要升级

当前 RAGFlow retrieval + 后端过滤已经能跑，但想要高召回、高准确，需要做多阶段检索：

```text
用户问题
→ query rewrite
→ query decomposition
→ 文档级摘要召回
→ 章节级召回
→ chunk 级召回
→ BM25 / keyword exact match
→ dense vector recall
→ reranker
→ evidence compression
→ answer generation
```

尤其对于企业知识库，不能只靠向量。因为企业问题里经常有：

```text
项目名
人名
日期
金额
接口名
版本号
指标名
文件名
```

这些内容关键词检索非常重要。

------

## 7. 右侧应该加“召回诊断”

你现在右侧有“处理流程”“文档治理”“缺口运营”。建议再加一个：

```text
召回诊断
```

每次回答后展示：

```text
原始问题
改写 query
召回文档数
召回 chunk 数
ACL 过滤前 / 过滤后
最高相似度
关键词覆盖率
最终引用数
是否触发低证据
```

因为 ChatService 已经保存 `RetrievalTrace`，其中包括 `query`、`rewritten_query`、`strategy`、`candidates`、`selected_context`。([GitHub](https://github.com/gumorty/RAG_0nly/blob/flow_6_27_1/RAG/backend/app/models/entities.py))
现在的问题是前端没有把它变成用户能理解的诊断面板。

------

# 五、你这四个需求的最终改造优先级

## P0：必须先做

```text
1. 新增 chat_sessions / chat_messages。
2. 前端支持知识库下新建、切换、删除、导出会话。
3. Chat API 增加 session_id。
4. answers 表保留，但挂到 chat_messages.answer_id。
5. knowledge_gaps 改成真实表，不再只从 answers 临时推导。
6. 缺口运营增加：误判、重跑、补资料、入评测集、已解决。
```

------

## P1：问答质量增强

```text
1. evidence_score 从 max similarity 改成多维评分。
2. trace 面板展示召回诊断。
3. 增加 query rewrite 质量记录。
4. 加 reranker。
5. 增加文档级摘要索引、章节级索引、chunk 级索引。
6. 对 Excel / JSON / Code / PPT / Image 做专门处理 profile。
```

------

## P2：资料摘要升级

```text
1. meeting-summary 改为 collection insights。
2. 摘要内容带引用。
3. 摘要项可点击追问。
4. 摘要可作为检索上层索引。
5. 按文档类型使用不同摘要模板。
```

------

## P3：Agent 化落地

```text
1. /api/agent/search
2. /api/agent/context-pack
3. /api/agent/paper-card
4. /api/agent/code-context
5. MCP Server
6. 给 Codex / Claude Code 调用
```

这样你的系统就不只是“人问知识库”，而是变成：

```text
企业知识库管理系统
+ 高质量 RAG 检索引擎
+ 缺口运营闭环
+ Agent 知识服务层
```

------

# 六、最关键的结论

你现在的系统已经把 RAGFlow 主链路打通了：创建知识库、上传资料、RAGFlow DeepDOC 解析、Embedding、索引、retrieval、后端 ACL 过滤、Chat 模型回答、引用和 trace 落库，这个方向是正确的。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_1/RAG))

但现在还缺 4 个产品级能力：

```text
1. 会话管理：现在只有 collection 级 answer 流，没有真正 session。
2. 缺口闭环：现在只是展示低证据，没有形成运营流程。
3. 摘要价值：现在只是 lab_signals 聚合，没有参与检索和问答。
4. 高质量多格式处理：现在要从“能解析”升级到“按格式结构化理解”。
```

如果你下一步开发，我建议按这个顺序做：

```text
第一步：chat_sessions / chat_messages
第二步：knowledge_gaps 独立表 + 缺口处理动作
第三步：召回诊断面板 + 多维 evidence_score
第四步：资料摘要改成可点击、带引用的知识库洞察
第五步：Excel / JSON / Code / Image / PPT 专门解析链路
```

这样改完以后，你这个项目才会真正从“RAGFlow 二次开发 Demo”变成“可落地的企业级 RAG 知识库管理平台”。