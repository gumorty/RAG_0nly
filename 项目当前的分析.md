我对 `flow_6_27_2/RAG` 分支做了静态审查，也查了 MinerU 当前能力。先给结论：

**你现在 PDF 表格问答不准，根因不只是“检索策略不好”，而是“表格结构没有被稳定地解析、结构化、切分、索引和召回”。MinerU 值得调研，而且很可能改善复杂 PDF、扫描件、多栏、表格、公式类资料的解析质量；但不能简单地把 MinerU 输出的 Markdown 直接丢进现有链路就期待完全解决。真正有效的方案是：把 MinerU 作为复杂文档解析引擎接入，然后把表格转成 row-level / cell-level / table-summary 结构化 chunk，再进入 RAGFlow 或你自己的检索层。**

------

# 1. 当前项目主链路判断

你当前 README 已经明确：项目主链路是“管理平台 + RAGFlow 引擎”。管理平台负责账号、权限、知识库、模型路由、审计和前端交互；RAGFlow 负责 DeepDOC 解析、切块、Embedding、索引和检索。主流程是“浏览器前端 → 管理平台 API → RAGFlow API → DeepDOC / Embedding / Elasticsearch → 管理平台调用 Chat 模型生成回答”。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_2/RAG))

你的 README 还写到：上传资料时，必要时会把 Excel / CSV / HTML / PPTX 等格式标准化为 RAGFlow 友好的内容；RAGFlow 执行 DeepDOC 解析、分块、Embedding 和索引；问答时优先调用 RAGFlow retrieval 获取证据，然后后端按本地 ACL 过滤 chunks，再调用当前启用的 Chat 模型生成答案。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_2/RAG))

这说明现在已经不是早期 Qdrant + worker + hash embedding 的本地 RAG 主链路。旧链路还保留在代码里，但当前默认主线是 RAGFlow。

------

# 2. 为什么你现在 PDF 表格回答不完整

你的测试现象是：上传 PDF，表格里的内容分析不清楚，回答不完整，有遗漏。这个问题很正常，因为当前系统对 PDF 表格还没有真正做到“表格语义化”。

## 2.1 PDF 当前走的是 RAGFlow 的 paper parser

你现在的 `chunk_method.py` 里，`.pdf` 被映射为 RAGFlow 的 `paper` 方法；`paper` 的说明是学术论文 / PDF 布局识别、OCR、outline 等。`.xlsx/.xls/.csv` 才走 `table` 方法；图片走 `picture`；PPT 走 `presentation`；JSON 走 `one`；代码文件基本走 `naive`。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/ragflow/chunk_method.py))

这意味着：

```text
PDF 里的表格 ≠ RAGFlow table parser 直接处理
PDF 里的表格 = paper parser 解析 PDF 页面时顺带识别出的表格片段
```

如果 PDF 表格被解析成 HTML table 片段，后端再把这些片段塞给 LLM，模型就可能只读到一部分行、一部分列，或者读到了 `<table><tr><td>` 这种结构化但不够友好的内容。你的 README 也已经把这个列为已知限制：表格型 PDF 的部分引用片段仍可能显示为 HTML table，后续需要做表格引用的结构化可读渲染。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_2/RAG))

## 2.2 当前检索是 chunk 级召回，不是表格级 / 行级召回

`RagFlowClient.retrieve()` 当前发送给 `/retrieval` 的 payload 包括：

```text
dataset_ids
question
page
page_size
top_k
similarity_threshold = 0.05
vector_similarity_weight = 0.3
highlight = false
```

并且 `top_k` 设为 `max(64, page_size * 8)`，最后返回 RAGFlow chunks。它还有 `retrieve_with_reranker()`，可以传 `rerank_id`、`similarity_threshold`、`vector_similarity_weight`，但主路径里如果没有主动使用 reranker，就还是普通 retrieval。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/ragflow/client.py))

对于普通段落问答，这种 chunk 级召回可以。但对表格问题，比如：

```text
“哪些地区有旺季上浮？”
“北京市、上海市、广州市的司局级标准分别是多少？”
“表格中所有其他人员住宿费低于 400 的地区有哪些？”
```

这类问题需要完整的表格结构、表头、行列关系和多行覆盖。单纯取 top 6 / top 8 chunks 很容易漏掉行。

## 2.3 后端有表格可读性处理，但不是表格索引

你现在 `ChatService` 里已经有 RAGFlow retrieval-only 路径：先 query rewrite，再调用 RAGFlow retrieve，之后做 ACL 过滤、证据准备、LLM 回答、保存 `RetrievalTrace` 和 `Answer`。代码里还对 RAGFlow chunk 做了 `_prepare_chunks_for_answer`、`_focus_evidence_for_question` 一类处理，用于把表格片段转得更可读、按问题词过滤行、增强证据聚焦。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/services/chat.py))

但这属于**回答前证据清洗**，不是**索引前表格结构化**。

也就是说：

```text
现在：RAGFlow 先把 PDF 表格解析成 chunk → 检索后再尽量清洗
理想：解析时就识别 table / row / column / cell → 按表格语义入库 → 检索时按字段召回
```

当前做的是后处理，能缓解，但不能根治。

## 2.4 你当前对 Excel 的处理也只是 Markdown 表格化

`document_normalize.py` 中，`.xlsx/.xlsm/.xls/.csv/.html/.pptx` 会被转成 RAGFlow 友好的 shadow 文件，比如 `.ragflow.md`。Excel 会通过 openpyxl/xlrd 读出，然后 `_rows_to_markdown()` 把第一行作为 header，后续行转成 Markdown table；超过 2000 行会截断。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/services/document_normalize.py))

这比直接上传原始 Excel 稳定，但仍然存在企业级表格问答的几个缺陷：

```text
1. 多级表头无法准确保留。
2. 合并单元格语义会丢失。
3. 单元格坐标、sheet、row、column 没有作为可检索元数据保存。
4. 数值和字段之间只是 Markdown 关系，不是结构化字段关系。
5. 长表会被截断。
```

所以表格问答不完整，不是偶然，而是当前架构自然会遇到的问题。

------

# 3. 是否应该接入 MinerU？

## 3.1 结论：值得接入，但不要直接替换 RAGFlow

MinerU 当前定位是面向 LLM / RAG / Agent 工作流的高精度文档解析引擎，可以把 PDF、DOCX、PPTX、XLSX、图片、网页转成结构化 Markdown / JSON，并且具备 VLM + OCR 双引擎和多语言 OCR 能力。([GitHub](https://github.com/opendatalab/MinerU))

它的官方能力里明确提到：

```text
1. 原生支持 DOCX、PPTX、XLSX。
2. 公式转 LaTeX。
3. 表格转 HTML。
4. 支持扫描件、手写、多栏布局、跨页表格合并。
5. 输出符合人类阅读顺序，并自动移除页眉页脚。
6. 支持 Markdown、JSON 和中间格式输出。
```

这些正好对应你当前 PDF 表格、扫描件、多格式解析不稳定的问题。([GitHub](https://github.com/opendatalab/MinerU))

MinerU 论文也说明它是为高精度文档内容提取设计，利用 PDF-Extract-Kit 模型，并通过预处理和后处理规则提升多类型文档提取质量。([arXiv](https://arxiv.org/abs/2409.18839?utm_source=chatgpt.com))

所以答案是：**可以调研 MinerU，而且建议你把它作为复杂文档解析增强模块接入。**

但注意，不建议现在直接把主链路从 RAGFlow 换成 MinerU。原因是：

```text
RAGFlow 已经承担 dataset、embedding、ES 索引、retrieval、文档状态等工程链路。
MinerU 更擅长复杂文档解析，不是完整知识库管理平台。
你当前最缺的是“解析增强 + 表格结构化 + 检索优化”，不是完全替换 RAGFlow。
```

更合适的架构是：

```text
普通文档：继续走 RAGFlow DeepDOC
复杂 PDF / 扫描件 / 表格型 PDF：先走 MinerU 解析
MinerU 输出 Markdown / JSON / HTML table
→ 管理系统做表格结构化转换
→ 再导入 RAGFlow 或进入自建 table index
→ 问答时融合检索结果
```

------

# 4. 当前代码中不同格式文档是怎么处理的

下面按实际项目链路讲。

------

## 4.1 上传与知识库创建

当前知识库创建后，后端会在 RAGFlow 中创建或复用 dataset，并把 `ragflow_dataset_id` 写入本地知识库元数据。上传资料后，管理平台会统一进入导入入口；必要时对 Excel、CSV、HTML、PPTX 等做 RAGFlow 友好化转换；然后 RAGFlow 执行解析、分块、Embedding 和索引。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_2/RAG))

所以当前上传后的主链路是：

```text
用户上传文件
→ 管理系统保存文档记录和元数据
→ 判断是否需要 normalize_for_ragflow
→ 选择 RAGFlow chunk_method
→ 上传到 RAGFlow dataset
→ update_document_parser
→ parse_documents
→ RAGFlow 内部解析、切块、Embedding、ES 索引
→ 管理系统同步文档状态
```

`RagFlowClient` 里已经实现了 dataset 创建、文档上传、parser 更新、触发解析、删除文档、list/get/update/delete chunk、retrieval 等接口。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/ragflow/client.py))

------

## 4.2 RAGFlow 友好化转换

`normalize_for_ragflow()` 的作用是：对于一些 RAGFlow 原生解析可能不稳定的格式，先在管理系统里转成 Markdown / CSV-like shadow 文件。代码注释里也写到，原始文件仍由管理平台保存，而 RAGFlow 接收 Markdown/CSV-like 表示，以保证 legacy spreadsheets、HTML exports、slide decks 的 parsing、chunking、embedding、retrieval 更稳定。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/services/document_normalize.py))

当前转换规则大致是：

```text
.xlsx / .xlsm → openpyxl → Markdown table
.xls → xlrd → Markdown table
.csv → Markdown table
.html / .htm → BeautifulSoup + markdownify
.pptx → python-pptx → 每页 slide 转 Markdown
其他格式 → 原样交给 RAGFlow
```

这个设计是对的，但仍然偏“文本化”，没有做到“结构化”。

------

## 4.3 本地 legacy parser 支持哪些格式

虽然当前默认主链路是 RAGFlow，但你的代码里仍然保留了本地 parser。`DocumentParser.parse()` 支持：

```text
.pdf
.docx
.doc
.pptx
.xlsx / .xlsm
.html / .htm
.md / .markdown
.txt
.csv
.json
```

不支持的类型会直接抛 `Unsupported file type`。解析后会调用 `normalize_text()`，如果没有抽取到可读文本就报错。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/rag/parsers.py))

具体解析方式：

```text
PDF：pypdf PdfReader，逐页 page.extract_text()，加 [Page N] 标记。
DOCX：python-docx，抽取段落和表格，表格 cell 用 “ | ” 拼接。
DOC：antiword 命令行解析。
PPTX：python-pptx，逐 slide 抽 shape.text。
XLSX：openpyxl 逐 sheet / row 抽取，行内 cell 用 “ | ” 拼接。
HTML：BeautifulSoup 去 script/style/noscript 后 markdownify。
TXT/MD/CSV/JSON：按 utf-8-sig、utf-8、chardet 解码。
```

这些解析适合基础文本抽取，但对 PDF 表格、扫描件、图片、复杂表格、代码语义不够。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/rag/parsers.py))

------

## 4.4 清洗

本地 parser 解析后会调用 `normalize_text()`。虽然当前 RAGFlow 主链路主要由 RAGFlow 完成解析，但本地 shadow 文件生成、legacy pipeline 和部分内容分析仍依赖清洗逻辑。

目前清洗主要解决：

```text
换行统一
空白字符统一
重复空行压缩
文本格式稳定
```

问题是：它属于通用文本清洗，不是结构级清洗。对表格来说，需要的是：

```text
表头识别
合并单元格展开
单位识别
行列坐标保留
跨页表格合并
caption / footnote 绑定
```

当前还没有这些能力。

------

## 4.5 切片 / 分块

当前主链路的分块由 RAGFlow 完成。你自己的 legacy `HierarchicalChunker` 仍然保留，它是：

```text
按 parsed.sections 遍历
section 内按空行分段
超过 max_tokens 就切 chunk
超长段落按中文/英文句号、问号、感叹号、换行切
保留 overlap
```

代码里可以看到 `HierarchicalChunker` 会为每个 chunk 保存 `content`、`normalized_content`、`title_path`、`token_count` 和 section metadata。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/rag/chunking.py))

这个切分适合普通文本，但不适合复杂表格。因为表格不是自然段落，不能简单按空行和句号切。表格需要的是：

```text
table summary chunk
row chunk
cell group chunk
column statistics chunk
```

否则问“哪些地区”“所有标准”“最高/最低”“按城市对比”时很容易漏召回。

------

## 4.6 向量化与入库

当前主链路中，Embedding 和索引由 RAGFlow 内部完成。README 里写明 RAGFlow 负责 DeepDOC 解析、分块、Embedding 和索引写入，RAGFlow storage 使用 Elasticsearch / MinIO / MySQL / Redis。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_2/RAG))

如果走 legacy local pipeline，则流程是：

```text
下载对象存储文件
DocumentParser 解析
删除旧 chunks
HierarchicalChunker 分块
生成 analysis
EmbeddingClient 向量化
写入 PostgreSQL chunks
写入 VectorStore
document.status = ready
```

legacy pipeline 里会把 `collection_id`、`document_id`、`chunk_id`、`title`、`filename`、`acl`、`project`、`meeting_date`、`title_path` 等 metadata 一起写入向量存储 payload。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/rag/pipeline.py))

但当前默认情况下，README 也说明旧的 Qdrant + worker 自研 RAG 链路已经放入 `legacy-local-rag` profile，不是默认主链路。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6_27_2/RAG))

------

## 4.7 检索、召回、LLM 回答

当前 Chat 主流程大致是：

```text
用户问题
→ 前端带 collection_id、session_id、history
→ 后端 ChatService
→ query rewrite
→ RAGFlow retrieve
→ RAGFlow 返回 chunks
→ 后端 ACL 过滤
→ 后端证据可读化 / 聚焦
→ LLM 基于证据回答
→ 保存 Answer、RetrievalTrace
→ 前端展示答案、引用、证据分
```

前端 API 里已经有 `listAnswers(collectionId, limit, sessionId)`、`chat`、`chatStream` 等接口，chatStream 使用 SSE 方式接收事件。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/frontend/app/api.ts))

前端现在还有 `session_id` 参数，但目前更多是“每个知识库一个 localStorage session id”，不等于完整的业务会话系统。`api.ts` 里还没有真正的 `listSessions / createSession / deleteSession / exportSession` 会话管理接口。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/frontend/app/api.ts))

------

# 5. 当前系统对企业级知识库的主要不足

## 5.1 对“文档格式”的支持还停留在能解析，不是能理解

当前支持多格式是对的，但企业级知识库要求更高：

```text
Word：标题层级、表格、批注、页眉页脚、修订痕迹
PDF：版面、页码、表格、图片、公式、扫描 OCR
Excel：sheet、表头、合并单元格、单位、行列坐标
JSON：JSONPath、字段说明、数组对象层级
代码：函数、类、依赖、调用关系、配置项
PPT：每页主题、图形、备注、图片 OCR
图片：OCR、图表、截图 UI 元素、caption
```

你当前 parser / normalizer 基本是“转成文本 / Markdown”，还没有为每类文档设计专用语义结构。

------

## 5.2 PDF 表格缺少结构化索引

现在 PDF 表格主要被当成普通 chunk。即使 RAGFlow/DeepDOC 抽出了 HTML table，后端也是在检索后把它转可读，而不是在入库前把它转成可检索的结构。

企业级表格问答应该至少保存：

```json
{
  "table_id": "doc1_page3_table2",
  "document_id": "...",
  "page": 3,
  "caption": "中央和国家机关工作人员赴地方差旅住宿费标准",
  "columns": ["地区", "部级", "司局级", "其他人员", "旺季"],
  "row_index": 12,
  "cells": {
    "地区": "北京市",
    "部级": "1100",
    "司局级": "650",
    "其他人员": "500",
    "旺季": "无"
  }
}
```

然后为每行生成自然语言 chunk：

```text
表格《中央和国家机关工作人员赴地方差旅住宿费标准》第 12 行：
地区为北京市，部级住宿费标准为 1100 元/人天，司局级为 650 元/人天，其他人员为 500 元/人天，旺季上浮为无。
```

这样问答才会稳定。

------

## 5.3 检索策略还没有按问题类型动态调整

现在 `retrieve()` 默认 similarity_threshold=0.05，vector_similarity_weight=0.3，page_size 根据调用方传入。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6_27_2/RAG/backend/app/ragflow/client.py))

但不同问题需要不同策略：

```text
事实定位：提高关键词权重，降低向量权重。
表格数值：优先 BM25 / keyword / field exact match。
摘要归纳：提高向量召回和 chunk 数。
跨文档比较：先文档级召回，再 chunk 级召回。
列举全部：需要更大的 top_k 和表格级扫描。
无答案判断：需要召回不足 + 覆盖不足判断。
```

现在还没有明显看到 `table_query_classifier` 或 `question_type_router` 这类模块。

------

## 5.4 evidence_score 仍然偏单一

目前 ChatService 里 evidence_score 大体来自召回 chunk 的最高分。这个分数能反映“有没有一个 chunk 相似”，但不能反映：

```text
是否覆盖所有问到的字段
是否召回完整表格
是否引用足够多文档
是否答案每个结论都有证据
是否表格行列匹配正确
是否有 ACL 过滤导致证据不足
```

企业级系统需要多维证据分，而不是单一 max similarity。

------

## 5.5 缺少表格专用评测集

你现在遇到“表格回答不完整”，最好的处理方式不是凭感觉调参数，而是建立固定评测集。

例如针对 PDF 表格设计：

```json
{
  "question": "北京市司局级住宿费标准是多少？",
  "expected_answer": "650 元/人天",
  "must_cite_document": "...pdf",
  "must_cite_table": "中央和国家机关工作人员赴地方差旅住宿费标准",
  "must_contain": ["北京市", "司局级", "650"],
  "should_refuse": false
}
```

再设计：

```text
单点查询
多行列举
跨页表格
包含旺季上浮
无答案拒答
表格摘要
最高/最低排序
```

没有评测集，就无法判断 MinerU 是否真的比 RAGFlow DeepDOC 好，也无法判断哪个策略更好。

------

# 6. MinerU 接入后效果会不会变化？

会，但要看你怎么接。

## 6.1 会明显改善的场景

MinerU 官方能力里明确包括表格转 HTML、复杂布局、多栏布局、扫描件 OCR、跨页表格合并、图片和图表解析等。([GitHub](https://github.com/opendatalab/MinerU))

所以它可能明显改善：

```text
1. 扫描 PDF 没有文字层。
2. PDF 表格结构复杂。
3. 多栏论文阅读顺序错乱。
4. 表格跨页。
5. 文档里有公式、图片、图表。
6. RAGFlow 返回 HTML table 片段难读。
```

## 6.2 不会自动解决的场景

如果你只是：

```text
PDF → MinerU Markdown → RAGFlow naive chunk → embedding → retrieval
```

那改善有限。因为表格虽然被 MinerU 转成了 HTML 或 Markdown，但如果后续仍然按普通文本 chunk 切，它仍然会出现：

```text
1. 长表被切断。
2. 表头和数据行分离。
3. 多行召回不完整。
4. 用户问“哪些”时只召回部分行。
5. 数值列和字段绑定不稳定。
```

所以 MinerU 必须配合表格结构化后处理。

------

# 7. 推荐改造方案：MinerU 作为复杂文档解析引擎

## 7.1 新增 ParserEngine 抽象

建议新增：

```text
backend/app/services/parse_engines/
├── base.py
├── ragflow_native.py
├── mineru_engine.py
└── parse_router.py
```

配置：

```env
PARSER_ENGINE=auto
MINERU_ENABLED=true
MINERU_BASE_URL=http://mineru:8000
MINERU_API_KEY=...
MINERU_TIMEOUT_SECONDS=300
MINERU_TRIGGER_EXTENSIONS=.pdf,.png,.jpg,.jpeg
MINERU_TABLE_MODE=true
```

路由策略：

```text
普通 PDF / Word / TXT / Markdown：RAGFlow 原生
扫描 PDF / 表格型 PDF / 用户选择高精度解析：MinerU
Excel：继续本地结构化 + RAGFlow table
图片：MinerU / OCR / RAGFlow picture
代码：本地 code parser
JSON：本地 JSONPath parser
```

------

## 7.2 MinerU 输出不能直接入库，要先做 Table Normalizer

MinerU 输出后，你应该保存：

```text
原始 PDF
MinerU markdown
MinerU JSON
layout 可视化结果
抽取出的 images
抽取出的 tables
```

然后对 table 做二次转换：

```text
HTML table / JSON table
→ 解析表头
→ 处理多级表头
→ 处理合并单元格
→ 生成 table_summary
→ 生成 row_chunks
→ 生成 cell_metadata
→ 写入 RAGFlow shadow markdown 或自建 table index
```

row chunk 示例：

```text
文档：W020200908376466568529.pdf
页码：第 3 页
表格：中央和国家机关工作人员赴地方差旅住宿费标准明细表
字段：地区、部级、司局级、其他人员、旺季上浮

地区：北京市；部级：1100 元/人天；司局级：650 元/人天；其他人员：500 元/人天；旺季上浮：无。
```

这样比原始 HTML table 更适合 Embedding 和关键词检索。

------

## 7.3 对 PDF 表格问题做专门检索

你需要加一个问题类型判断：

```text
table_point_query：问某个单元格
table_list_query：问哪些行满足条件
table_compare_query：多地区/多指标对比
table_summary_query：总结整张表
normal_text_query：普通文档问答
```

不同问题走不同 retrieval：

### 单点表格查询

```text
query: 北京市 司局级 住宿费
→ exact keyword / BM25 优先
→ row chunk 召回
→ 返回 1-3 行
```

### 列举类问题

```text
query: 哪些地区有旺季上浮
→ 先召回 table_summary
→ 再召回整张表或全部相关 row chunks
→ 不要只取 top 6
```

### 汇总类问题

```text
query: 总结这张表的地区差异
→ 召回 table_summary + representative rows
→ LLM 汇总
```

------

## 7.4 RAGFlow 仍然可以继续作为索引引擎

你不需要立刻自建完整向量库。短期可以：

```text
MinerU 解析 PDF
→ 生成 enhanced markdown / row-level markdown
→ 上传这个 shadow markdown 到 RAGFlow
→ RAGFlow 负责 embedding / ES / retrieval
```

但是要注意：

```text
原始 PDF 保存到 MinIO
RAGFlow 入库的是 MinerU 增强版 shadow 文档
metadata 里记录 original_filename / parser_engine / parse_artifact
```

这样你保留 RAGFlow 工程能力，又增强了复杂文档解析。

------

# 8. 检索策略建议立即修改的点

## 8.1 表格问题增大召回数

当前 retrieval 如果只取 6 或 8 个 chunks，很容易漏表格行。建议表格问题动态设置：

```text
page_size = 20 或 30
top_k = 128 或 256
similarity_threshold = 0.0 ~ 0.05
```

先召回更多，再在后端精排和压缩。

## 8.2 使用 retrieve_with_reranker

`RagFlowClient` 已经有 `retrieve_with_reranker()`，但你需要在 ChatService 主链路里真正接入：

```python
client.retrieve_with_reranker(
    dataset_id=dataset_id,
    question=query,
    page_size=dynamic_page_size,
    rerank_id=settings.ragflow_rerank_id,
    similarity_threshold=strategy.similarity_threshold,
    vector_similarity_weight=strategy.vector_similarity_weight,
)
```

Reranker 对“表格行是否真的回答问题”会比单纯 embedding similarity 更有效。

## 8.3 不要对“列举全部”问题过度裁剪

你现在 `_focus_evidence_for_question` 会根据 query terms 聚焦行。这个对单点查询有帮助，但对“哪些地区”“所有”“分别”“汇总”这类问题可能会误删相关行。

建议加判断：

```text
如果问题包含：
哪些、所有、分别、汇总、列表、对比、最高、最低、全部

则不要只保留包含 query term 的行；
应该保留整张表摘要 + 更多行。
```

## 8.4 evidence_score 改成多维

建议：

```text
final_score =
0.25 * max_similarity
+ 0.20 * rerank_score
+ 0.20 * keyword/entity coverage
+ 0.15 * citation coverage
+ 0.10 * table field coverage
+ 0.10 * answer grounding score
```

并在 trace 中保存：

```json
{
  "score_breakdown": {
    "max_similarity": 0.57,
    "rerank_score": 0.71,
    "keyword_coverage": 0.8,
    "table_field_coverage": 0.6,
    "citation_count": 3,
    "unique_doc_count": 2
  }
}
```

------

# 9. 企业级知识库相比，你当前还缺什么

## 9.1 文档处理层

当前：多格式可以解析或转 Markdown。
企业级：要对不同格式做语义结构化。

需要补：

```text
PDF：layout + OCR + table + figure + page coordinate
Excel：sheet + row + column + merged header + unit
JSON：JSONPath + schema + nested object chunk
代码：language parser + symbol index + dependency graph
PPT：slide + notes + image OCR + chart understanding
图片：OCR + VLM caption + region metadata
```

## 9.2 索引层

当前：主要依赖 RAGFlow chunk + embedding + ES。
企业级：需要多层索引。

建议：

```text
document_summary_index
section_summary_index
table_summary_index
row_chunk_index
normal_chunk_index
entity_keyword_index
code_symbol_index
```

## 9.3 检索层

当前：RAGFlow retrieval + 后端 ACL filter + 简单证据准备。
企业级：需要检索编排。

建议：

```text
query rewrite
query classification
query decomposition
multi-index retrieval
BM25 + dense hybrid
reranker
evidence compression
answer grounding check
no-answer detection
```

## 9.4 评测层

当前：有 evalsets 基础，但表格评测不足。
企业级：需要按业务场景建立固定评测集。

建议至少建立：

```text
普通文本事实问答
PDF 表格问答
Excel 数值问答
跨文档对比
无答案拒答
扫描件 OCR
图片/图表问答
代码知识问答
```

每条评测要有：

```json
{
  "question": "...",
  "expected_answer_points": [],
  "must_cite_documents": [],
  "must_cite_tables": [],
  "forbidden_claims": [],
  "should_refuse": false
}
```

## 9.5 可观测性

当前已经保存 Answer 和 RetrievalTrace，但前端诊断还不够。
企业级需要一个“召回诊断面板”：

```text
原始问题
改写 query
RAGFlow raw chunks
ACL 过滤前后数量
表格解析方式
parser_engine
chunk_method
similarity / keyword / rerank 分数
最终引用
为什么判定证据不足
```

------

# 10. 我建议你下一步按这个路线做

## 第一阶段：先做 A/B 评测，不急着全量替换

选 5 份你的真实问题文档：

```text
1. 普通 PDF
2. 表格型 PDF
3. 扫描 PDF
4. Excel 表格
5. 带图表的论文 PDF
```

为每份设计 10 个问题：

```text
单点表格查询
多行列举
跨页表格
摘要
无答案拒答
```

对比：

```text
RAGFlow native
MinerU markdown shadow
MinerU table row chunks
```

指标：

```text
答案正确率
必须字段命中率
引用准确率
表格行召回率
遗漏率
解析耗时
部署成本
```

## 第二阶段：接入 MinerU 作为可选 parser

不是替换 RAGFlow，而是加：

```text
parse_engine = ragflow_native | mineru | auto
```

前端上传时可以选择：

```text
普通解析
高精度表格解析
扫描件 OCR 解析
```

## 第三阶段：做表格结构化 chunk

把 MinerU / RAGFlow 输出的 table HTML 转成：

```text
table_summary_chunk
row_chunks
cell metadata
```

再入库。

## 第四阶段：改造检索策略

增加：

```text
table_query_classifier
dynamic_top_k
reranker
table-aware evidence merge
multi-dimensional evidence_score
```

## 第五阶段：做企业级评测闭环

把所有“回答不完整”的测试问题加入 evalset，后续每次改 parser 或 retrieval 都跑回归。

------

# 11. 最终结论

你现在遇到的问题，不是单纯调 prompt 或 top_k 就能彻底解决。**PDF 表格问答不完整，本质是解析层、切分层、索引层和检索层都还没有表格语义化。**

MinerU 值得接入，因为它对复杂 PDF、扫描件、多栏布局、表格、公式、图片这些场景确实更强，官方也明确支持表格转 HTML、跨页表格合并、扫描件 OCR、多格式输出和 FastAPI / Docker 部署。([GitHub](https://github.com/opendatalab/MinerU))

但正确方案不是：

```text
RAGFlow 不行 → 换成 MinerU
```

而是：

```text
RAGFlow 继续作为知识库索引和 retrieval 引擎
MinerU 作为复杂文档解析增强引擎
管理系统负责表格结构化、评测、检索编排和证据治理
```

你下一步最应该做的是：

```text
1. 建立 PDF 表格评测集。
2. 接入 MinerU 做 A/B 解析对比。
3. 把表格转成 row-level 结构化 chunk。
4. 表格问题动态扩大召回并启用 reranker。
5. 在 trace 中展示召回诊断和表格字段覆盖率。
```

这样你的系统才能从“能解析资料的 RAG 知识库”，升级为“能稳定处理企业复杂文档、表格、扫描件和多格式资料的企业级知识库管理系统”。