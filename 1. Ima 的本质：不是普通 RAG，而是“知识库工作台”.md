可以调研。先说明边界：**腾讯没有公开 Ima 知识库的完整后端源码和算法细节**，所以只能基于官方产品信息、App Store 更新记录、腾讯云公开 RAG 技术文档、以及公开产品报道来做“可验证事实 + 工程推断”。

## 1. Ima 的本质：不是普通 RAG，而是“知识库工作台”

Ima 官方定位是“以知识库为基础的 AI 工作台”，核心体验是“搜、读、写”一体化。它支持个人知识库、共享知识库、知识库广场、任务模式、录音纪要、笔记、AI 解读、文档问答自动匹配图表等功能；个人知识库的数据来源包括本地文件、微信文件、公众号文章、网页、图片、音频等。([App Store](https://apps.apple.com/cn/app/ima-腾讯-ai-工作台/id6737188438))

所以 Ima 不是简单的：

> 上传文档 → 切 chunk → 向量化 → 问答

而更像：

> 多来源知识采集 → 文档/图片/音频解析 → 知识组织 → 多索引检索 → 大模型生成 → 笔记/报告/PPT/播客/共享知识库复用

这点对你们自己的 RAG 系统很重要：**Ima 的强点不是单个检索算法，而是把“知识进入、知识整理、知识检索、知识再生产”做成了闭环。**

------

## 2. Ima 知识库的核心流程推断

### 流程一：多源数据接入

Ima 支持本地文件、微信文件、公众号文章、网页、图片、音频等内容进入个人知识库。共享知识库也支持把 Doc、PDF、网页、公众号文章、图片、笔记、AI 问答结果加入知识库。([App Store](https://apps.apple.com/cn/app/ima-腾讯-ai-工作台/id6737188438))

工程上它大概率会抽象成统一的 `KnowledgeItem`：

```text
KnowledgeItem
- item_id
- source_type: file / wechat_file / article / webpage / image / audio / note / qa_result
- raw_uri: 原始文件或链接地址
- title
- owner_id
- kb_id
- folder_id
- tags
- permission
- created_at / updated_at
- parsed_status
```

这一步的关键不是向量化，而是**把各种来源统一成可管理的知识对象**。你们自己的系统如果只做“文件上传”，就会比 Ima 少一层非常关键的“知识收纳入口”。

------

### 流程二：解析层：文本、版面、表格、图片、音频都要结构化

腾讯云公开的企业级 RAG 文档把标准 RAG 离线链路拆成：文档采集、文档解析、文本分块、向量化、索引存储；在线链路拆成：查询理解、向量检索、重排序、上下文组装、答案生成。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

对 Ima 这种产品来说，解析层至少要处理：

```text
PDF / Word / PPT / Excel / Markdown / TXT / HTML / 图片 / 音频
```

腾讯云 RAG 文档也明确指出，PDF、Word、PPT、Excel、网页、扫描件/图片各自有解析难点，比如 PDF 多栏布局错乱、表格结构丢失、图片文字无法提取，Excel 合并单元格和公式结果丢失，扫描件需要 OCR。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

所以 Ima 的解析层大概率包括：

```text
文件类型识别
→ PDF/Office/HTML/Markdown/TXT 解析
→ 图片 OCR
→ 音频 ASR 转写
→ 版面结构识别
→ 表格结构保留
→ 图片/图表关联
→ 标题、段落、页码、章节元数据抽取
```

尤其是你现在最关心的 PDF 表格问题，Ima 很可能不是只把 PDF 当纯文本处理，而是把表格、图片、页码、章节都纳入结构化索引。腾讯云文档里也强调，表格解析需要“自动识别并保留表格结构”，OCR 用于扫描件和图片。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

------

### 流程三：知识组织层：知识库、文件夹、标签、共享权限

Ima 的一个很关键设计是：它不是把所有 chunk 平铺到一个大库里，而是让用户用**知识库、文件夹、标签**组织资料。2025 年 4 月的 Ima 工作日志显示，个人知识库支持创建文件夹和多级管理，知识库支持标签筛选和管理，文件也支持重命名。([腾讯云](https://cloud.tencent.com/developer/news/2423098))

这意味着 Ima 的检索不只是向量相似度，还会有明显的元数据过滤：

```text
kb_id 过滤
folder_id 过滤
tag 过滤
source_type 过滤
owner / member 权限过滤
更新时间过滤
```

公开文章还提到，Ima 允许终端用户通过标签限定 RAG 检索范围，通过目录组织知识库文件；文章作者也分析说，目录结构可以作为一种结构型元数据，用来帮助系统先定位相关目录分支，再在该范围内检索。([腾讯云](https://cloud.tencent.com/developer/article/2525850))

这点非常适合你们自己的系统借鉴：**不要只靠 embedding 相似度，要把目录、标签、文档类型、页码、表格行号、权限都作为检索约束。**

------

### 流程四：索引层：大概率是“多索引混合检索”

生产级 RAG 不应该只做向量检索。腾讯云 RAG 文档明确说，单一向量检索难以覆盖所有查询类型，生产级方案通常需要混合检索；比如产品型号、编号、政策名这类问题，关键词匹配往往比语义检索更准。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

公开文档给出的典型混合检索配置包括：

```text
向量检索：适合语义相似问题
关键词 / BM25：适合型号、编号、术语、精确匹配
元数据过滤：适合时间、部门、文档类型、标签筛选
```

腾讯云也称其智能体开发平台知识库模块内置混合检索，支持向量检索与关键词检索自动融合。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

所以 Ima 的知识库检索大概率不是单路向量，而是类似：

```text
用户问题
→ 查询理解 / 查询改写
→ 判断检索范围：当前知识库 / @多个知识库 / 标签 / 文件夹 / 全网
→ 权限过滤
→ 向量召回
→ 关键词召回
→ 可能的网页/公众号搜索召回
→ 表格/图片/音频转写内容召回
→ rerank 精排
→ 组装上下文
→ 调用模型生成
```

------

### 流程五：生成层：混元 + DeepSeek + Copilot / Agent 能力

Ima 官网搜索结果和公开报道显示，Ima 已接入腾讯混元大模型和 DeepSeek-R1；2025 年 2 月报道也提到，用户在使用搜、读、写和知识库时可以选择腾讯混元或 DeepSeek-R1。([ima.qq.com](https://ima.qq.com/?utm_source=chatgpt.com))

到 2026 年 6 月的 App Store 版本记录里，Ima 已经进一步支持 copilot、MCP 连接、首选模型、自定义模型、知识库列表检索、跨知识库内容移动和复制等能力。([App Store](https://apps.apple.com/cn/app/ima-腾讯-ai-工作台/id6737188438))

这说明 Ima 正从“知识库问答工具”升级成：

```text
RAG 知识库 + Agent 任务执行 + MCP 工具连接 + 多模型路由
```

也就是说，它的问答生成不是固定模板，而是可能根据任务类型选择不同模式：

```text
普通问答：知识库 RAG
长文档解读：文档结构化摘要
复杂任务：任务拆解 + 多轮检索 + 生成报告/PPT/播客
当前页面问答：基于当前文件/网页上下文
共享知识库问答：基于团队知识库 + 权限过滤
```

------

## 3. Ima 知识库的完整架构可以这样理解

你可以把 Ima 拆成 8 层：

```text
1. 多端入口层
   PC / Mac / App / 小程序 / 微信导入 / 网页导入

2. 知识对象层
   文件、网页、公众号文章、图片、音频、笔记、AI问答结果

3. 解析加工层
   PDF解析、Office解析、HTML清洗、OCR、ASR、表格识别、图文关联

4. 知识治理层
   知识库、共享知识库、文件夹、标签、重命名、权限、成员管理

5. 索引层
   向量索引、关键词索引、元数据索引、表格索引、图片/音频转写索引

6. 检索层
   查询理解、范围判断、混合召回、重排序、上下文压缩

7. 生成层
   混元 / DeepSeek / 自定义模型，基于上下文生成答案、报告、PPT、播客、测验

8. 反馈与再沉淀层
   结果加入笔记、问答结果入库、共享知识库复用、知识库广场传播
```

它最值得你们借鉴的不是某一个模型，而是这几个设计：

```text
多源接入
结构化解析
元数据治理
混合检索
权限过滤
多知识库协作
生成结果再入库
```

------

## 4. 对你们自己的 RAG 系统有什么启发？

结合你之前的项目问题，尤其是 PDF 表格解析不准、召回不稳定，我建议你们不要只优化 embedding，而是按 Ima 的思路补齐这几块。

### 第一，文档解析要升级为“结构化解析”

你们现在重点应该继续做：

```text
pdf_probe.py：判断是否扫描 PDF
mineru.py：自动走 OCR / 版面解析
table_index.py：Markdown 表格生成行级索引
parser_router.py：把 MinerU Markdown 后追加表格行索引
```

原因很明确：腾讯云 RAG 文档也指出，文档解析质量对最终效果影响很大，表格结构丢失会导致后续再好的向量模型也拿不到完整信息。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

你们应该把 PDF 解析输出从纯文本升级为：

```text
Document
Page
Section
Paragraph
Table
TableRow
Figure
Caption
```

尤其表格要有：

```text
table_id
row_id
page_no
headers
row_text
markdown_row
source_bbox 可选
```

------

### 第二，检索要从“向量检索”升级为“混合检索 + 元数据过滤”

你们现在如果只做向量 TopK，遇到这些问题会不稳定：

```text
型号、编号、日期、金额、政策条款
表格中的某一行
Excel / PDF 表格单元格
跨章节总结
```

应该升级为：

```text
向量召回
+ BM25 / keyword 召回
+ 表格行级召回
+ 元数据过滤
+ rerank 精排
```

腾讯云文档也明确把混合检索、查询扩展、同义词映射、重排序模型、元数据过滤列为检索策略调优方向。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

你们的表格问题可以单独走一个轻量路由：

```text
如果问题包含：
“表格 / 指标 / 数值 / 金额 / 占比 / 参数 / 对比 / 哪一项 / 多少”
→ 提高 table_row chunk 的召回权重
→ 同时召回表格标题、表头、所在页上下文
```

------

### 第三，知识库需要“目录 + 标签 + 权限”三套元数据

Ima 的文件夹、标签、共享知识库设计说明它非常重视知识组织。([腾讯云](https://cloud.tencent.com/developer/news/2423098))

你们企业级 RAG 系统也应该有：

```text
知识库级：kb_id、部门、业务线、权限
目录级：folder_path、parent_folder
文件级：file_type、version、owner、effective_date
chunk级：page_no、section_title、table_id、row_id
标签级：产品、流程、合同、财务、技术文档、FAQ
```

查询时不要直接全库检索，而是：

```text
先判断检索范围
→ 再做权限过滤
→ 再做混合召回
```

这会明显降低“召回到不相关文档”的概率。

------

### 第四，答案必须带证据链

企业级 RAG 不能只回答，还要能证明答案来自哪里。腾讯云文档也把“引用标注、置信度评分、兜底策略、答案一致性”列为回答质量控制重点。([Tencent Cloud ADP](https://adp.tencentcloud.com/zh/blog/enterprise-rag-guide))

你们应该让答案输出变成：

```text
答案
证据来源：
- 文件名
- 页码
- 表格名 / 行号
- 原文片段
- 置信度
```

如果没有足够证据，就回答：

```text
当前知识库未检索到足够依据，不能确定。
```

这比“强行编一个答案”更适合企业场景。

------

## 5. Ima 和你们当前 RAG 系统的差距可以这样对照

| 能力     | Ima 公开表现                                     | 你们系统建议补齐                                            |
| -------- | ------------------------------------------------ | ----------------------------------------------------------- |
| 多源接入 | 文件、微信文件、公众号、网页、图片、音频         | 先支持 PDF、DOCX、Excel、PPT、MD、网页，再扩展微信/企业微信 |
| 文档解析 | 支持文档、图片、音频解读，文档问答可匹配图表     | 强化 MinerU、OCR、表格结构、图片说明                        |
| 知识组织 | 个人知识库、共享知识库、文件夹、标签、知识库广场 | 增加 kb/folder/tag/version/permission 元数据                |
| 检索     | 大概率混合检索 + 范围过滤                        | 向量 + BM25 + 表格行索引 + rerank                           |
| 协作     | 共享知识库、管理员、成员权限                     | 企业多租户、部门权限、文档可见性控制                        |
| 生成     | 问答、报告、PPT、播客、测验、笔记                | 先做好问答证据，再做报告/PPT生成                            |
| Agent 化 | copilot、MCP、自定义模型、skill                  | 后续接 MCP、工具调用、任务拆解                              |

------

## 6. 你们可以按这个路线复刻 Ima 的核心能力

第一阶段，先把 RAG 基础打稳：

```text
文件上传
→ 文档解析
→ OCR 判断
→ 表格结构化
→ chunk
→ embedding
→ BM25
→ hybrid retrieval
→ rerank
→ answer with citations
```

第二阶段，做知识组织：

```text
多个知识库
目录
标签
文件版本
权限
共享知识库
```

第三阶段，做 Ima 式工作台：

```text
AI 笔记
问答结果加入知识库
报告生成
PPT 生成
知识库分享
知识库模板
```

第四阶段，再做 Agent：

```text
查询意图判断
自动选择知识库
多轮检索
任务拆解
MCP 工具调用
企业系统连接
```

------

## 结论

Ima 知识库的核心不是“用了哪个向量库”或者“用了哪个大模型”，而是这套闭环：

```text
多源知识进入
→ 高质量解析
→ 结构化知识治理
→ 混合检索
→ 权限与范围控制
→ 大模型生成
→ 结果再次沉淀
→ 共享与协作
```

你们现在最应该优先追的不是 Ima 的全部功能，而是它背后的三个关键工程能力：

```text
1. PDF / 表格 / 图片的高质量解析
2. 标签 / 目录 / 权限驱动的检索范围控制
3. 向量 + BM25 + 表格行索引 + rerank 的混合检索
```

只要这三块做好，你们自己的 RAG 系统在企业文档问答场景里会比单纯“文档切片 + 向量库 + LLM”稳定很多。

下面是我对 **Tencent/WeKnora** 的调研结论，并把它和 **腾讯 Ima 知识库** 放在一起分析。

## 一句话结论

**WeKnora 可以理解为腾讯开源出来的一套“企业级知识库 / RAG / Agent / Wiki”工程框架；Ima 是面向普通用户和团队的产品化 AI 工作台。**

更严谨一点说：**目前没有官方材料明确说明 WeKnora 就是 Ima 的后端源码或完整技术内核**。但从功能结构看，WeKnora 和 Ima 的设计范式高度一致：都是围绕“多源知识进入 → 文档理解 → 知识库组织 → 混合检索 → 大模型回答 / Agent 任务 → 结果沉淀”来做的。

WeKnora 官方 README 说明它是面向企业级文档理解、语义检索和智能推理的开源知识框架，核心能力包括 RAG 快速问答、ReAct Agent、多源接入、自动 Wiki、MCP、Web Search、多模型、多租户 RBAC、私有化部署等。([GitHub](https://github.com/Tencent/WeKnora)) Ima 官方 App Store 描述则把 Ima 定位为“以知识库为基础的 AI 工作台”，提供个人知识库、共享知识库、知识库广场、任务模式、录音纪要、笔记、AI 解读、图文并茂问答等能力。([App Store](https://apps.apple.com/cn/app/ima-腾讯-ai-工作台/id6737188438?utm_source=chatgpt.com))

------

# 1. WeKnora 是什么？

WeKnora 的官方定位是：

> 把原始文档转成可查询的 RAG、可自主推理的 Agent，以及可自维护的 Wiki。

从 README 看，它不是一个简单的“上传 PDF + 向量库 + Chat”的 Demo，而是一个完整的知识平台。官方强调三大核心能力：**RAG Quick Q&A、ReAct Agent、Wiki Mode**；同时支持飞书、Notion、语雀、RSS 等多源接入，支持 10+ 文档格式，兼容 OpenAI、DeepSeek、Qwen、智谱、混元、Gemini、MiniMax、Ollama 等模型。([GitHub](https://github.com/Tencent/WeKnora))

从仓库目录看，它也不是单体脚本，而是一个完整工程：包含 `docreader` 文档解析服务、`frontend` 前端、`internal` 后端核心逻辑、`mcp-server`、`migrations`、`miniprogram`、`skills/preloaded`、`helm`、`docker`、`deploy` 等模块。([GitHub](https://github.com/Tencent/WeKnora))

可以把 WeKnora 理解为：

```text
企业级知识库系统
= 文档解析系统
+ RAG 检索系统
+ Agent 推理系统
+ Wiki 知识组织系统
+ 多租户权限系统
+ 多模型 / 多向量库 / 多存储后端适配层
```

------

# 2. WeKnora 的核心流程

## 2.1 数据接入层：不只是上传文件，而是多源同步

WeKnora 支持从外部平台自动导入和同步内容到知识库。官方数据源文档写到，它支持飞书、企业微信、Notion、Confluence 等外部平台，数据源绑定到知识库，一个知识库可以接入多个数据源，凭证使用 AES-256-GCM 加密存储。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/数据源导入开发文档.md))

它的数据源同步不是简单爬一下文档，而是有完整的同步策略：

```text
数据源连接
→ 测试连接
→ 资源发现
→ 选择同步资源
→ 配置增量 / 全量同步
→ 配置 Cron 同步频率
→ 配置冲突策略
→ 进入异步任务队列
→ 文档解析 / 分块 / 向量化 / 索引
```

官方文档里明确提到，飞书知识库同步支持 docx、doc、sheet、bitable、file 类型文档；同步策略包括增量同步、全量同步、同步频率、覆盖/跳过、同步删除等。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/数据源导入开发文档.md)) 数据源导入架构里还写到了 Adapter Pattern、Registry Pattern、Strategy Pattern、Producer-Consumer、Cursor-based Pagination，这说明它把不同平台的差异抽象成统一连接器接口，而不是为每个平台写死一套逻辑。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/数据源导入开发文档.md))

这点和 Ima 很像。Ima 也不是只允许用户上传本地文档，它支持本地文件、微信文件、公众号文章、网页、图片、音频等进入个人知识库。([App Store](https://apps.apple.com/cn/app/ima-腾讯-ai-工作台/id6737188438?utm_source=chatgpt.com))

------

## 2.2 文档解析层：DocReader 是核心模块

WeKnora 里有一个独立的 `docreader` 服务。官方文档说明，DocReader 是 WeKnora 中负责文档解析和处理的 gRPC 服务，支持多种文档格式读取、OCR 识别、多模态处理等功能。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docreader/README.md))

DocReader 的关键点有几个：

```text
1. 独立 gRPC 服务
2. 支持对象存储，例如 MinIO、COS、OSS
3. 可配置 MinerU 作为高级文档解析服务
4. 支持扫描 PDF 渲染为图片
5. OCR / VLM 能力由 Go App 侧调用
6. 支持文件大小、并发、DPI、JPEG 质量等资源控制
```

官方文档明确说，`MINERU_ENDPOINT` 是可选的高级文档解析服务，用来支持更复杂的文档结构识别和处理。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docreader/README.md)) 这和你们现在正在做的 `pdf_probe.py + mineru.py + table_index.py` 方向高度一致。

尤其重要的是，DocReader 文档提到：扫描 PDF 会被渲染为 JPEG 图片，然后交由 Go App 侧调用 OCR / VLM 服务处理。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docreader/README.md)) 这说明 WeKnora 的解析链路不是“纯文本抽取”，而是把扫描件、图片、多模态内容纳入了文档理解流程。

2026 年 6 月的 Changelog 还显示，WeKnora 新增了 OpenDataLoader 和 PaddleOCR-VL 解析引擎，优化了扫描 PDF 的流式图片结果、独立重型异步队列、Excel/PPT 专用转换器、PPTX 媒体抽取、Markdown 表格归一化等能力。([GitHub](https://github.com/Tencent/WeKnora/blob/main/CHANGELOG.md))

这对你们的 RAG 系统非常关键：**PDF 表格回答不准，本质上不是 LLM 不够强，而是解析层没有把表格、版面、页码、图片、标题结构稳定保留下来。**

------

## 2.3 分块层：自适应三层 Chunking

WeKnora 专门有一份 Chunking 文档。它说明 RAG 的效果强依赖文档切片方式，chunk size、overlap、切分边界都会直接影响召回和回答质量。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/CHUNKING.md))

它采用的是 **Adaptive 3-tier chunking**：

```text
auto      ：自动判断文档结构，选择最合适策略
heading   ：适合 Markdown / 标题结构明显的文档
heuristic ：适合 PDF 式结构，如分页、编号章节、多语言章节标题
legacy    ：兜底递归切分
```

官方文档还提到，heading 策略会在 embedding 时给 chunk 加上面包屑上下文，例如 `# Top > ## Section`，heuristic 策略会利用分页符、编号章节、多语言章节标记、大写标题、视觉分隔符等结构信号。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/CHUNKING.md))

这对你们系统有直接启发：你们不能所有文档都用一种固定 chunk 策略。至少应该区分：

```text
普通纯文本：递归切分
Markdown / Word：标题层级切分
PDF：页码 + 标题 + 段落切分
表格：表格级 + 行级 chunk
FAQ：问答对原子 chunk
长报告：父子 chunk
```

------

## 2.4 索引层：混合检索，不只是向量检索

WeKnora README 里列出的检索策略包括：

```text
BM25 稀疏召回
Dense 稠密召回
GraphRAG
父子分块
pgvector HNSW 加速
多维度索引
```

并且支持 PostgreSQL pgvector、Elasticsearch、OpenSearch、Milvus、Weaviate、Qdrant、Apache Doris、腾讯云 VectorDB 等向量存储。([GitHub](https://github.com/Tencent/WeKnora))

它还提供了“如何集成新的向量数据库”的文档，要求实现标准化的 `RetrieveEngine` 接口，包括 `Retrieve` 检索方法和 `Support` 支持的检索类型。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/使用其他向量数据库.md))

这说明 WeKnora 的设计重点是：**检索引擎可插拔，向量库可替换，召回策略可组合。**

这正是企业级 RAG 和简单 Demo 的区别。

简单 Demo：

```text
query → embedding → vector topK → LLM
```

WeKnora 式流程：

```text
query
→ 查询改写 / 意图判断
→ 权限过滤 / 知识库范围过滤
→ BM25 关键词召回
→ Dense 向量召回
→ GraphRAG 关系扩展
→ Parent-child chunk 补上下文
→ Rerank
→ 上下文组装
→ LLM / Agent 生成
→ 引用证据返回
```

Changelog 里还提到它接入了腾讯云 LKEAP Rerank，并且在 rerank API 失败时会回退到原始检索结果。([GitHub](https://github.com/Tencent/WeKnora/blob/main/CHANGELOG.md)) 这说明它考虑了生产系统里的稳定性问题，而不是把所有能力都假设为永远可用。

------

## 2.5 生成层：RAG 问答 + ReAct Agent + Wiki Mode

WeKnora 的生成层分三类：

```text
1. Quick Q&A
   基于知识库的快速问答

2. ReAct Agent
   自主编排知识检索、MCP 工具、Web Search，完成复杂多步任务

3. Wiki Mode
   从原始文档中自动生成结构化、相互链接的 Markdown Wiki 页面和知识图谱
```

这些能力在 README 里有明确说明。([GitHub](https://github.com/Tencent/WeKnora))

这和 Ima 的产品形态也非常接近。Ima 有 copilot、任务模式、AI 解读、报告 / 播客 / PPT 生成、思维导图、知识小测验等能力。([App Store](https://apps.apple.com/cn/app/ima-腾讯-ai-工作台/id6737188438?utm_source=chatgpt.com)) 从工程角度看，这些功能背后大概率都不是单次 RAG，而是：

```text
任务理解
→ 拆解步骤
→ 多轮检索
→ 多源资料聚合
→ 结构化生成
→ 输出为报告 / PPT / 播客 / 笔记 / 测验
```

WeKnora 的 ReAct Agent 和 Wiki Mode 就是开源侧能看到的类似实现方向。

------

## 2.6 知识组织层：知识库、标签、共享空间、RBAC

WeKnora 的知识管理不只是“文件列表”。README 里说明它支持 FAQ / Document / Wiki 三种知识库类型，支持文件夹导入、URL 导入、多标签管理、在线录入、批量重新解析等能力。([GitHub](https://github.com/Tencent/WeKnora))

它的共享空间文档说明，共享空间用于跨租户协作，可以共享知识库、共享智能体、访问他人共享的知识库，并在对话和智能体场景中使用这些共享资源。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/共享空间说明.md))

RBAC 文档里则说明，WeKnora 的租户内权限模型包括 Viewer、Contributor、Admin、Owner 四级角色，分别对应只读提问、上传维护自己资源、租户运维、租户所有者等能力。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/RBAC说明.md))

这正好对应 Ima 的“个人知识库 + 共享知识库 + 知识库广场”的产品方向。Ima 更偏消费者和学习/办公产品体验；WeKnora 更偏企业私有化和开发者可扩展底座。

------

# 3. WeKnora 和 Ima 知识库的关系分析

我建议你这样理解二者关系：

```text
Ima = 面向用户的 AI 知识工作台产品
WeKnora = 面向开发者 / 企业的开源知识框架
```

它们不是完全等价，但底层思想高度一致。

| 对比项     | Ima 知识库                                                 | WeKnora                                                      |
| ---------- | ---------------------------------------------------------- | ------------------------------------------------------------ |
| 产品定位   | 腾讯 AI 工作台，主打搜、读、写                             | 开源 LLM 知识框架，主打 RAG、Agent、Wiki                     |
| 用户对象   | 普通用户、学生、研究者、办公团队                           | 开发者、企业、私有化部署团队                                 |
| 数据入口   | 本地文件、微信文件、公众号、网页、图片、音频               | 文件、URL、文件夹、飞书、Notion、语雀、RSS、IM、Chrome 插件等 |
| 知识库形态 | 个人知识库、共享知识库、知识库广场                         | FAQ / Document / Wiki，支持共享空间和多租户                  |
| 检索方式   | 未公开完整细节，产品上体现为知识库问答、图文问答、任务模式 | 明确支持 BM25、Dense、GraphRAG、父子分块、多向量库           |
| Agent 能力 | Copilot、任务模式、报告 / PPT / 播客生成                   | ReAct Agent、MCP、Web Search、Agent Skills、Wiki Mode        |
| 协作能力   | 共享知识库、知识库广场                                     | 共享空间、RBAC、多租户、审计日志                             |
| 部署形态   | 腾讯产品，云端服务                                         | 可本地 / Docker / Kubernetes / 私有云部署                    |
| 可改造性   | 用户侧不可改源码                                           | 开源可二次开发                                               |

所以，**WeKnora 更适合作为你们自研 RAG 系统参考的工程样板；Ima 更适合作为产品体验和功能闭环的参考对象。**

------

# 4. Ima 背后的核心链路，用 WeKnora 可以大致还原

结合 Ima 的公开功能和 WeKnora 的开源架构，可以推测 Ima 知识库的大致链路是：

```text
多源内容进入
  本地文件 / 微信文件 / 公众号 / 网页 / 图片 / 音频

→ 内容解析
  PDF / Office / HTML / OCR / ASR / 图片理解 / 表格识别

→ 知识组织
  个人知识库 / 共享知识库 / 标签 / 文件夹 / 权限 / 知识库广场

→ 多路索引
  向量索引 / 关键词索引 / 表格索引 / 图片 OCR 文本 / 音频转写文本 / 元数据索引

→ 智能检索
  知识库范围判断 / 查询改写 / 混合召回 / Rerank / 上下文压缩

→ 大模型生成
  问答 / 总结 / 思维导图 / PPT / 报告 / 播客 / 知识小测验

→ 结果沉淀
  笔记 / 问答结果入库 / 共享知识库 / 知识库广场复用
```

WeKnora 开源出来的部分，基本覆盖了其中的这些核心后端能力：

```text
文档解析
分块
向量化
混合检索
Rerank
Agent
MCP
Web Search
Wiki
共享空间
RBAC
多租户
可观测性
评估
```

Ima 产品层额外做得更强的是：

```text
微信生态接入
消费者级交互体验
知识库广场分发
笔记体验
录音纪要
PPT / 播客 / 小测验等内容生产能力
移动端体验
```

------

# 5. 对你们自己 RAG 项目的直接启发

结合你之前的项目问题，尤其是 PDF、表格、召回不准，我建议你们重点参考 WeKnora 的这些设计。

## 5.1 文档解析要服务化

你们现在可以继续沿着这个方向做：

```text
pdf_probe.py
→ 判断是否扫描 PDF

mineru.py
→ OCR / 高级版面解析

parser_router.py
→ 根据文件类型和解析难度选择解析器

table_index.py
→ Markdown 表格生成行级索引
```

但更进一步，建议把解析层独立成类似 WeKnora DocReader 的服务：

```text
rag-docreader
- parse_pdf
- parse_docx
- parse_excel
- parse_ppt
- parse_image
- parse_audio
- render_scanned_pdf_to_images
- call_ocr_or_vlm
- return structured markdown + metadata
```

WeKnora 的 DocReader 是独立 gRPC 服务，并支持 MinerU、存储后端、扫描 PDF 渲染、OCR / VLM 处理，这个设计非常适合你们参考。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docreader/README.md))

------

## 5.2 表格问题要单独建索引，不要混在普通文本 chunk 里

WeKnora 的 Changelog 里专门提到了 Markdown 表格归一化、PaddleOCR-VL HTML table normalization，以及“Markdown table rows passed raw to rerank”这类修复。([GitHub](https://github.com/Tencent/WeKnora/blob/main/CHANGELOG.md))

这说明表格在 RAG 里是一个真实的工程难点。

你们现在做的“表格生成行级索引”是正确方向。建议最后形成这样的结构：

```text
table_chunk
- table_id
- table_title
- page_no
- headers
- row_id
- row_text
- markdown_row
- source_file
- bbox 可选
```

召回时走：

```text
普通文本召回
+ BM25 精确词召回
+ 表格行级召回
+ 表头 / 表题上下文补齐
+ rerank
```

表格问题的关键不是“多给 LLM 一点上下文”，而是让检索阶段能精确命中“哪张表的哪一行”。

------

## 5.3 Chunking 要从固定切分升级为自适应切分

你们可以参考 WeKnora 的三层策略：

```text
heading：适合 markdown / word 标题结构
heuristic：适合 PDF 页码、章节编号、视觉分隔
legacy：兜底递归切分
```

同时增加父子 chunk：

```text
parent chunk：章节 / 页面 / 表格整体
child chunk：段落 / 表格行 / FAQ 原子问答
```

问答时先召回 child，再把 parent 带回去补上下文。这对长 PDF、制度文档、技术手册尤其有效。

------

## 5.4 检索一定要混合召回

你们当前如果只是向量召回，遇到表格、编号、日期、金额、参数、政策条款一定会不稳定。

建议升级为：

```text
Dense Vector TopK
+ BM25 TopK
+ Table Row TopK
+ Metadata Filter
+ Rerank
+ Answer with Citation
```

WeKnora 明确支持 BM25、Dense、GraphRAG、父子分块、HNSW pgvector、多维度索引等策略。([GitHub](https://github.com/Tencent/WeKnora)) 这说明企业级知识库不能只依赖单一路径召回。

------

## 5.5 知识库要有“组织能力”

Ima 的强点不是单个 RAG 算法，而是知识可以被组织、共享、复用。WeKnora 也有共享空间、多租户 RBAC、知识库共享、智能体共享等能力。([GitHub](https://github.com/Tencent/WeKnora/blob/main/docs/共享空间说明.md))

你们自己的系统也应该至少有：

```text
kb_id
folder_path
tags
source_type
owner_id
tenant_id
permission
version
effective_date
page_no
section_title
table_id
row_id
```

否则知识越多，召回越混乱。

------

# 6. 你们可以参考的目标架构

建议你们最终把自己的 RAG 系统拆成这几层：

```text
1. 接入层
   文件上传、URL 导入、企业微信/飞书/Notion/语雀同步

2. 解析层
   PDF/Office/Excel/PPT/图片/音频解析
   MinerU/OCR/VLM/ASR
   表格结构化

3. 清洗与结构化层
   Markdown 规范化
   表格行级索引
   图片 caption
   页码/章节/标题元数据

4. Chunking 层
   heading chunk
   heuristic PDF chunk
   recursive chunk
   parent-child chunk
   table-row chunk

5. 索引层
   向量索引
   BM25 索引
   表格索引
   元数据索引
   可选 GraphRAG

6. 检索层
   查询改写
   元数据过滤
   混合召回
   rerank
   上下文压缩

7. 生成层
   普通 RAG 问答
   多轮问答
   Agent 任务
   报告 / PPT / 摘要生成

8. 治理层
   多知识库
   标签
   文件夹
   权限
   共享空间
   审计日志

9. 评估层
   Recall@K
   Hit Rate
   MRR
   Answer Faithfulness
   Citation Accuracy
   表格问答专项测试
```

------

# 7. 最重要的判断

**WeKnora 对你们最有价值的地方，不是拿来直接替换你们项目，而是作为“企业级 RAG 工程标准答案”去拆解。**

你们现在最应该借鉴它的 5 个点：

```text
1. DocReader 式独立文档解析服务
2. MinerU / OCR / VLM / ASR 的可配置解析引擎
3. 自适应 Chunking + parent-child chunk
4. BM25 + Dense + 表格行级索引 + Rerank 的混合检索
5. 多知识库 / 标签 / 共享空间 / RBAC 的知识治理能力
```

如果要和 Ima 对标，你们不要一开始就做“知识库广场、播客、PPT、AI 生图”这些产品层功能。应该先把底座做到稳定：

```text
高质量解析
高质量切分
高质量召回
证据可追溯
权限可控制
结果可评估
```

这套底座做好之后，再往 Ima 式 AI 工作台扩展，才不会变成“功能很多，但回答不准”的系统。