# RAG帝王分析

## 一、采用横纵分析说明

**横向分析**：同层级模块拆解、并列对比、组成结构，看**RAG 由什么构成、各环节是什么**。

**纵向分析**：发展演进、层级递进、技术迭代，看**RAG 从低级到高级怎么进化、底层逻辑是什么**。

------

# 二、横向分析：RAG 架构维度（并列结构拆解）

把 RAG 拆成**五大横向核心模块**，每个模块独立存在、各司其职，构成完整链路：

### 1. 知识库层（数据源底座）

- 类型：私有文档、PDF/Word/ 网页、数据库、知识库、企业内部资料
- 作用：提供**外部真实知识**，弥补大模型参数知识局限
- 核心问题：数据清洗、格式统一、去重脱敏

### 2. 分块预处理层（Chunking）

- 方式：固定长度分块、语义分块、层级分块、递归分块
- 作用：把长文本切为适合向量检索的小段，兼顾**上下文完整性**与**检索精度**
- 痛点：分块过大冗余、过小断语义

### 3. 向量嵌入与向量库层（Embedding + Vector DB）

- 嵌入模型：BGE、Sentence-BERT、M3E 等
- 向量库：FAISS、Milvus、Qdrant、Chroma
- 作用：文本转向量，建立**语义索引**，支持相似度召回

### 4. 检索与重排层（Retrieval + Rerank）

- 初检索：语义检索、关键词检索、混合检索（稀疏 + 稠密）
- 重排：Cross-BERT、BGE-Reranker 对初筛结果二次精排
- 作用：从海量块里**先粗召、再精筛**，过滤无关内容

### 5. 大模型生成层（LLM Generation）

- 流程：把检索到的上下文 + 用户问题 组装 Prompt
- 能力：基于检索内容作答、引用溯源、减少幻觉
- 约束：Prompt 工程、上下文窗口限制、拒答无用信息

------

# 三、纵向分析：RAG 技术演进层级（从初级到高阶递进）

## 第一层：朴素 RAG（Naive RAG）

- 流程：简单切块 → 全局向量检索 → 直接拼 Prompt 生成
- 特点：架构最简、易落地
- 缺陷：分块粗暴、检索不准、上下文割裂、幻觉仍严重
- 适用：新手入门、简单知识库 Demo

## 第二层：高级 RAG（Advanced RAG）

- 新增技术：语义分块、混合检索、重排器、Query 改写、上下文压缩
- 特点：优化**检索质量**和**query 理解**
- 提升：召回准确率大幅提升，回答关联性更强
- 适用：企业常规落地、大部分业务场景

## 第三层：模块化 / 智能 RAG（Modular/Agentic RAG）

- 新增技术：
  1. 层级索引、父块 - 子块检索
  2. 多轮 Query 改写、意图识别
  3. Agent 规划、工具调用、自我反思
  4. 知识图谱 + 向量融合检索
- 特点：**自主规划、多步检索、自我校验**
- 上限：能处理复杂推理、多文档关联、跨领域问答
- 适用：复杂企业问答、专业知识库、智能助手

### 