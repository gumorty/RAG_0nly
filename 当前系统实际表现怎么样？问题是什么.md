结合你截图里的实际效果和 `flow_6-29_1` 分支代码来看，当前系统**正文类问答、表格类问答已经有明显进步**，但是对“图、图表、页面定位、视觉内容”的处理还不够完整。你现在的问题不是简单改 prompt 能解决的，而是需要补齐 **图像/图表解析、页码索引、图表专用召回、证据清洗** 这几条链路。

下面我按“实际表现 → 根因 → 必须修复的链路”来分析。

------

# 一、从你截图看，当前系统实际表现怎么样？

## 1. 表 3 问题：表现相对最好

你问：

> 在人工智能发展报告的表3中，对于多模态生成类型中有哪些技术路线分别对应哪些类型？

你的系统回答出了：

```text
扩散模型：
- Stability AI Stable Diffusion
- OpenAI DALL·E
- OpenAI Sora
- 快手 KLING
- Runway Gen-3

端到端理解与生成架构：
- 谷歌 Gemini
- OpenAI GPT-4o
```

这个回答基本是可用的。原因是这类问题属于**表格文本型问题**，你现在的 `table_index.py` 和 `table_row` 检索通道已经发挥作用了。代码里 `parser_router.py` 会在 MinerU 输出 Markdown 后追加表格行级索引；普通 Markdown 标准化分支也会调用 `_augment_markdown_if_enabled()` 追加表格索引。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/parser_router.py))

也就是说，**表格类事实召回已经初步有效**。

------

## 2. 图 7 问题：答案方向对，但证据链有污染

你问：

> 第44页有一个图七，在这个图中风险对于哪些模块？

你的系统回答了核心治理要素、风险识别维度、活动全流程、全链条角色等内容，方向上接近 IMA 的回答。

但是截图里的证据卡片有一个非常严重的问题：

```text
以下是从长表格/长片段中按当前问题筛出的相关行。
如果某个地区行没有明确给出旺季期间或上浮金额，不得从相邻地区推断。
```

这个提示明显是之前“差旅住宿费标准表”场景的 guardrail，和《人工智能发展报告》完全无关。这个内容出现在 AI 报告的证据中，说明你的证据清洗/聚焦逻辑存在**领域规则泄漏**。

从代码看，`_focus_evidence_for_question()` 会对长表格/长片段统一追加“如果某个地区行没有明确给出旺季期间或上浮金额，不得从相邻地区推断”这类提示，而不是只在“差旅住宿费标准表”文档里使用。相关逻辑在 `chat.py` 的证据聚焦和 `_known_policy_table_repair()` 附近。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/chat.py))

这会导致两个问题：

```text
1. 证据内容被系统规则污染；
2. LLM 可能把无关规则当成知识库证据。
```

这是 P0 级问题，必须修。

------

## 3. 图 6 问题：你的系统没有真正读到图本身

你问：

> 第35页有一个图六，我想知道这个图中大概表述了什么？

IMA 回答里给出了非常具体的图表信息：

```text
研发设计 35.7%
中试验证 18.8%
生产制造 18.8%
营销服务 45.5%
运营管理 45.5%
```

而你的系统回答的是：

```text
图6为“基于百个优秀案例统计的AI应用产业链分布”
整体呈现“两端快，中间慢”
产业链两端落地较快，中间环节较慢
```

这个回答**不是错**，但明显不完整。它更像是从正文概括和图目录里推断出来的，而不是从图 6 的具体图表数据中读出来的。

从截图证据看，top 证据主要是：

```text
图目录/表目录
图6 基于百个优秀案例统计的AI应用产业链分布……35
```

以及一些正文片段。没有看到真正包含图 6 数据的视觉解析结果。这说明你的系统当前对“图”类问题的处理主要依赖：

```text
图标题
图目录
正文描述
```

而不是：

```text
图像内容 OCR / VLM 图表理解 / 图表结构化数据
```

这就是你和 IMA 差距最明显的地方。

------

# 二、当前代码链路的核心问题

你的系统现在主链路是：

```text
管理平台
  -> RAGFlow API
  -> DeepDoc / MinerU / Embedding / Elasticsearch
  -> 后端基于证据调用 Chat 模型回答
```

README 里也说明了：RAGFlow 负责 DeepDoc 解析、chunk、Embedding、索引和混合检索，最终回答由管理平台基于证据调用当前 Chat 模型生成。([GitHub](https://github.com/gumorty/RAG_0nly/tree/flow_6-29_1/RAG))

这个架构方向没问题。真正的问题在于下面这些链路还不完整。

------

# 三、从导入开始逐链路检查问题

## 1. 导入阶段：PDF 被转换成单个 `.mineru.md`，视觉资产丢失

当前 `parser_router.py` 的逻辑是：

```text
PDF
  -> MinerU parse_file()
  -> result.markdown
  -> append table_index
  -> 返回 filename.pdf.mineru.md
  -> 上传 RAGFlow
```

代码里确实是把 MinerU 结果作为 Markdown 文件返回给 RAGFlow。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/parser_router.py))

而 `mineru.py` 当前下载结果 zip 后，主要提取的是 `full.md` 或其他 `.md` 文件。代码没有把 zip 里的图片、版面 JSON、图表区域、figure asset 单独取出来做结构化处理。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/mineru.py))

这就导致：

```text
正文和表格：能进入 Markdown
图片和图表：大概率只剩标题/附近文字
图中柱状图、流程图、架构图：没有被结构化成可检索证据
```

所以你的系统能回答“图6叫什么”，但很难回答“图6里每个环节占比是多少”。

------

## 2. 解析阶段：你有 table_index，但没有 figure_index

你现在已经有：

```text
table_index.py
```

能够把 Markdown/HTML 表格变成：

```text
表格N 第M行：字段=值；字段=值
```

这就是为什么表 3 问题答得不错。

但你现在缺少对应的：

```text
figure_index.py
```

也就是缺少下面这种图像/图表索引：

```text
图6：
页码：第35页
标题：基于百个优秀案例统计的AI应用产业链分布
图类型：柱状图/统计图
核心数据：
- 研发设计：35.7%
- 中试验证：18.8%
- 生产制造：18.8%
- 营销服务：45.5%
- 运营管理：45.5%
图意：AI应用在营销服务和运营管理环节分布最多，中试验证和生产制造较少。
```

IMA 之所以能答得更细，我推断它至少有较强的图像 OCR / VLM 图表理解 / 页面级视觉解析能力。你不一定要复刻它，但你的企业级 RAG 系统必须补上“图表索引”这一层。

------

## 3. 清洗阶段：差旅表 guardrail 泄漏到了 AI 报告

这是当前最明显的 bug。

你当前证据里出现了：

```text
如果某个地区行没有明确给出旺季期间或上浮金额，不得从相邻地区推断。
```

这个规则只适用于之前的差旅住宿费 PDF，不应该出现在《人工智能发展报告》中。

问题根因是：`chat.py` 的 `_focus_evidence_for_question()` 对所有长表格/长片段统一追加了这个 guardrail。相关逻辑和 `_known_policy_table_repair()` 在同一个证据聚焦流程中。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/chat.py))

必须改成：

```text
只有当文档类型 = 差旅住宿费标准表
或者证据中明确包含“差旅住宿费 / 旺季上浮 / 司局级 / 部级 / 其他人员”
才允许使用差旅规则。
```

否则你会出现“知识库证据污染”，这是企业级系统不能接受的问题。

------

## 4. 切块阶段：图目录 chunk 排名过高，真实图内容 chunk 不存在或排不上来

你截图里图 6 问题的证据主要命中了：

```text
图目录
图6 标题
正文概述
```

这说明当前 chunk 层存在两个问题：

```text
1. 图目录 chunk 被当成强相关证据；
2. 真正图像内容没有单独 chunk，或者没有被召回。
```

当前 RAGFlow dataset 默认 parser config 包含 `layout_recognize: DeepDOC`、`chunk_token_num`、delimiter、parent-child 等配置。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/ragflow/client.py))

但是因为你上传给 RAGFlow 的已经是 `.mineru.md`，而不是原始 PDF，RAGFlow 后续更像是在处理 Markdown 文本。它不再能充分利用原始 PDF 图像区域。也就是说：

```text
PDF 原始视觉信息
  -> MinerU full.md
  -> RAGFlow Markdown naive chunk
```

这个过程中，图像本体信息很可能已经丢了。

------

## 5. 向量化/入库阶段：缺少 page_no、figure_no、figure_type 等结构化元数据

现在 RAGFlow 检索返回 chunk 后，你会转换成 `RetrievedChunk`，里面有 chunk_id、document_id、title、content、score、metadata 等字段。RAGFlow client 的 retrieve 接口会设置 top_k、similarity_threshold、vector_similarity_weight 等参数。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/ragflow/client.py))

但是对于图类问题，仅有普通 content 不够。你需要在入库时增加：

```text
page_no
figure_no
figure_title
figure_type
source_image_uri
ocr_text
vlm_summary
chart_data
```

否则用户问：

```text
第35页的图六讲了什么？
```

系统无法做确定性的页面和图号定位，只能依赖语义召回。

------

## 6. 检索阶段：没有 figure 专用 intent/channel

现在 `retrieval_channels.py` 里已经有：

```text
semantic
keyword_bm25
rewrite_x
synonym_expansion
table_row
section
title
local_bm25_cache
```

其中 `detect_query_intent()` 会识别表格、数值、定义、流程、章节、代码等意图，并且表格/数值意图会加 `table_row` 通道。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/retrieval_channels.py))

但是它没有识别：

```text
图
图6
图七
图表
第35页
第44页
图中
示意图
架构图
流程图
```

所以你的“图六/图七”问题没有进入专门的 figure retrieval channel。它会被当成普通语义问题、表格问题或章节问题处理。

这就是为什么图 6 问题召回到了图目录，而不是图内容。

------

## 7. 本地 BM25 cache 只能检索文本，不能解决视觉图表问题

你现在有 `chunk_cache.py`，会同步 RAGFlow chunk 到本地 `RetrievalChunkCache`，并支持本地 BM25 检索。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/chunk_cache.py))

这个设计很好，适合作为 RAGFlow 失败兜底，也适合关键词召回。

但它目前缓存的是：

```text
chunk 文本
标题
title_path
page_no
metadata
```

如果图表没有被解析成文本，那么 BM25 也搜不到图表内容。

所以图类问题的核心仍然是：

```text
先把图变成文本/结构化数据
再谈检索和召回
```

------

# 四、当前最需要修复的问题优先级

## P0：修复证据污染

你现在必须先把差旅住宿费的 guardrail 从通用证据聚焦中移除。

当前不合理现象：

```text
AI 报告证据中出现“地区、旺季、上浮金额、相邻地区”等差旅规则。
```

建议改成：

```python
def _domain_guardrail_for_question(question, focused, metadata=None):
    if _is_travel_policy_question(question, focused, metadata):
        return "如果某个地区行没有明确给出旺季期间或上浮金额，不得从相邻地区推断。"
    return ""
```

判断条件必须严格：

```text
问题或证据中同时出现：
差旅 / 住宿费 / 司局级 / 部级 / 旺季 / 上浮
```

不能只因为它是“表格/长片段”就加这个规则。

------

## P0：新增图表索引 `figure_index.py`

你现在有 `table_index.py`，下一步必须新增：

```text
backend/app/services/figure_index.py
```

它至少要生成：

```markdown
# 图表检索索引

图6：
页码：第35页
标题：基于百个优秀案例统计的AI应用产业链分布
图类型：统计图/柱状图
图中关键数据：
- 研发设计：35.7%
- 中试验证：18.8%
- 生产制造：18.8%
- 营销服务：45.5%
- 运营管理：45.5%
图意：AI应用在营销服务和运营管理环节分布最多，中试验证和生产制造环节较少。
```

对图 7：

```markdown
图7：
页码：第44页
标题：人工智能风险管理体系
核心模块：
- AI风险管理模块：风险识别、风险评估、风险应对
- AI流程管控模块：AI技术提供方、AI应用提供方、AI应用使用方
涉及对象：
- 数据
- 算法模型
- 算力
- 规划设计
- 数据处理
- 模型建设
- 测试验证
- 部署上线
- 输入、输出、使用方式、使用意图
```

这样图类问题就不会只依赖目录和正文。

------

## P0：不要只取 MinerU 的 `full.md`，要解析 zip 里的图像/布局产物

当前 `mineru.py` 只提取 Markdown。([GitHub](https://raw.githubusercontent.com/gumorty/RAG_0nly/flow_6-29_1/RAG/backend/app/services/mineru.py))

你应该改成：

```text
MinerU zip
  -> full.md
  -> content_list.json / layout json
  -> images/
  -> tables/
  -> figures/
```

具体文件名要以 MinerU 实际 zip 内容为准，不要假设固定名称。实现上可以先打印和保存：

```python
archive.namelist()
```

然后把结果写到 metadata：

```json
{
  "mineru_zip_files": [...],
  "mineru_image_count": 12,
  "mineru_json_files": [...]
}
```

如果 zip 里有图片，就保存到 MinIO，再让 VLM 对图片生成说明。

------

## P0：增加图类问题检索通道

在 `retrieval_channels.py` 里增加：

```python
def detect_query_intent(question: str) -> str:
    if re.search(r"第?\s*\d+\s*页.*图|图\s*[一二三四五六七八九十\d]+|图表|示意图|架构图|流程图", question):
        return "figure"
```

然后新增 channel：

```python
if intent == "figure":
    channels.append(
        RetrievalChannel(
            name="figure",
            query=f"{question} 图表检索索引 图号 页码 标题 图中内容 图表数据 概述",
            vector_similarity_weight=0.2,
            similarity_threshold=0.0,
            page_size=max(base_page_size, 16),
            rrf_weight=1.2,
            description="图表/视觉内容召回",
        )
    )
```

同时增加同义词：

```python
"图": ["图表", "插图", "示意图", "架构图", "流程图", "统计图", "图中内容"]
"图六": ["图6", "Figure 6"]
"图七": ["图7", "Figure 7"]
```

------

## P0：增加图类 rerank，压低图目录 chunk

你现在图 6 问题 top 证据命中了图目录。图目录只能说明“有这个图”，不能说明“图里有什么”。

所以需要在 rerank 中区分：

```text
图目录 chunk：弱证据
图表索引 chunk：强证据
正文解释 chunk：中等证据
```

增加：

```python
def _rerank_for_figure_question(question, chunks):
    if not _looks_like_figure_question(question):
        return chunks

    target_page = extract_page_no(question)
    target_figure = extract_figure_no(question)

    def score(chunk):
        base = chunk.score
        content = chunk.content or ""
        bonus = 0

        if "# 图表检索索引" in content:
            bonus += 0.25
        if target_figure and target_figure in content:
            bonus += 0.18
        if target_page and f"第{target_page}页" in content:
            bonus += 0.15
        if "图目录" in content and "# 图表检索索引" not in content:
            bonus -= 0.12
        if any(x in content for x in ["占比", "%", "模块", "流程", "体系", "架构"]):
            bonus += 0.08

        return base + bonus
```

这一步会直接改善“图目录排第一”的问题。

------

# 五、企业级链路应该改成什么样？

你现在的链路是：

```text
PDF
  -> MinerU
  -> full.md
  -> table_index
  -> RAGFlow
  -> RAGFlow retrieval
  -> 后端重排
  -> LLM 回答
```

建议升级为：

```text
PDF 上传
  ↓
PDF 预检
  ↓
MinerU / DeepDOC 解析
  ↓
解析产物拆分
    ├── pages.md：页级正文
    ├── tables.md：表格行级事实
    ├── figures.md：图表索引与视觉摘要
    ├── outline.md：目录、章节、图目录、表目录
    └── raw_assets：图片、表格、布局 JSON
  ↓
清洗
    ├── 去页眉页脚
    ├── 去重复目录
    ├── 去跨领域 hardcode guardrail
    └── 统一页码、图号、表号
  ↓
切块
    ├── 正文 chunk
    ├── 表格 row chunk
    ├── 图表 figure chunk
    ├── 页面 page chunk
    └── 章节 summary chunk
  ↓
向量化/入库
    ├── RAGFlow 混合索引
    ├── 本地 BM25 cache
    └── metadata: page_no / figure_no / table_id / section
  ↓
检索
    ├── semantic channel
    ├── keyword channel
    ├── table_row channel
    ├── figure channel
    ├── page channel
    └── title/section channel
  ↓
RRF 融合 + intent rerank
  ↓
证据聚焦
  ↓
LLM 只基于证据回答
```

------

# 六、针对你这三个测试问题，修复后的预期表现

## 图 6 问题

用户问：

```text
在人工智能发展报告的文档中第35页有一个图六，我想知道这个图中大概表述了什么？
```

理想证据 top1 应该是：

```text
# 图表检索索引
图6
页码：第35页
标题：基于百个优秀案例统计的AI应用产业链分布
...
```

而不是：

```text
图目录：图6……35
```

理想答案应该包含：

```text
图6展示了基于百个优秀案例统计的AI应用在产业链环节中的分布。
其中营销服务和运营管理占比较高，研发设计次之，中试验证和生产制造较低。
具体占比包括研发设计35.7%、中试验证18.8%、生产制造18.8%、营销服务45.5%、运营管理45.5%。
```

------

## 图 7 问题

用户问：

```text
第44页图七中风险对于哪些模块？
```

理想证据 top1 应该是：

```text
# 图表检索索引
图7
页码：第44页
标题：人工智能风险管理体系
```

理想答案应该包含：

```text
图7主要涉及两类模块：

1. 风险管理模块：
风险识别、风险评估、风险应对。

2. 流程管控/全链条角色模块：
AI技术提供方、AI应用提供方、AI应用使用方。

其中技术提供方涉及数据、算法模型、算力；
应用提供方涉及规划设计、数据处理、模型建设、测试验证、部署环境、部署上线等；
应用使用方涉及输入、输出、使用方式、使用意图。
```

并且证据里绝对不能再出现：

```text
旺季
上浮金额
相邻地区
住宿费
```

------

## 表 3 问题

这个问题当前已经比较好。修复后要保证不要退化。

理想证据仍然应该来自：

```text
# 表格行级检索索引
表3 多模态模型技术路线表
```

答案应继续保留：

```text
扩散模型：Stable Diffusion、DALL·E、Sora、KLING、Runway Gen-3
端到端理解与生成架构：Gemini、GPT-4o
```

------

# 七、你现在需要立刻做的修改清单

## 第一组：马上修 bug

```text
1. 修改 chat.py
   - 移除通用差旅 guardrail
   - 只在差旅住宿费文档/问题中启用该规则

2. 修改 _focus_evidence_for_question()
   - 不要对所有长表格都追加“地区/旺季/上浮”提示

3. 增加测试
   - AI报告证据中不得出现“旺季/住宿费/相邻地区”
```

------

## 第二组：补图表能力

```text
1. 新增 figure_index.py
   - 解析图号、图标题、页码、附近正文
   - 支持图目录和正文图标题配对
   - 支持 VLM/OCR 图像摘要

2. 修改 mineru.py
   - 不只提取 full.md
   - 记录 zip 中图片/JSON 文件
   - 把 figure asset 保存到 MinIO 或本地临时目录

3. 修改 parser_router.py
   - Markdown 后追加 table_index
   - 再追加 figure_index

4. 修改 retrieval_channels.py
   - 增加 figure intent
   - 增加 figure channel

5. 修改 chat.py
   - 增加 figure rerank
   - 降低图目录 chunk 权重
   - 提升图表索引 chunk 权重
```

------

## 第三组：增强页码定位

```text
1. 增加 page_no 解析
   - 识别“第35页”“第44页”
   - 中文数字“图六/图七”转为图6/图7

2. 在 Markdown 中插入页级 marker
   - ## 第35页
   - ## 第44页

3. 在 chunk metadata 中保留 page_no
```

------

## 第四组：企业级评测

你现在应该新增这几类评测指标：

```text
FigureRecall@5：
图类问题 top5 是否命中图表索引 chunk。

PageHit@5：
带页码问题 top5 是否命中对应页码。

EvidencePollutionRate：
证据中是否出现无关领域 guardrail，比如 AI 报告中出现“住宿费/旺季”。

TOCOverRankRate：
top1 是否只是目录 chunk，而不是正文/图表内容 chunk。

FigureDataCompleteness：
图表问题是否回答出图中的关键数值或模块。
```

这比单纯看“回答像不像”更企业级。

------

# 八、最终判断

你当前系统已经具备：

```text
PDF OCR 判断
MinerU 解析
表格行级索引
多通道召回
RRF 融合
本地 BM25 cache
后端证据聚焦
```

这些方向是对的。

但从你和 IMA 的对比看，当前短板非常明确：

```text
1. 图像/图表内容没有结构化入库；
2. 页码和图号没有成为强检索信号；
3. 图目录 chunk 排名过高；
4. 差旅表 guardrail 污染了其他文档证据；
5. 当前检索偏“文本/表格”，还没有“视觉图表”通道。
```

所以接下来不要再优先堆 prompt，也不要只调 similarity threshold。你应该优先补齐：

```text
figure_index.py
+ figure retrieval channel
+ page-aware rerank
+ visual/VLM summary
+ evidence pollution guardrail
```

这几个做完后，你的系统在“第几页第几个图讲了什么”“图中有哪些模块”“图中有哪些数值”这类企业文档问题上，才会接近 IMA 这种表现，同时保留你自己系统的权限、审计、可观测和可评测能力。